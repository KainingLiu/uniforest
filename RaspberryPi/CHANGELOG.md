# 变更与验证记录

当前操作方法与参数以 [README.md](README.md) 和源码为准。本文件保留变更日期、
适用范围及当时验证结果；历史通过记录不代表当前全量测试通过。

## 2026-09-26

### 文档整合与代码发布

| 行为变化 / 工作项 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 合并重复的动作、速度和部署说明，保留历史记录 | 根 README、上下位机 README、PROJECT、ACTIONS 及工具说明 | 相对链接与源码核对；前臂 +12° 对应范围修正为 12～132° |
| 汇总待发布的既有修改：Task1 投放顶墙 180° 航向校准、Tag6 三帧滤波及平移 Ki/Kd=0 | competition.py | 两任务两轮配置；Python 语法、导入与协议检查 |
| 汇总既有步进 400/60/400 与 Grap3 回收 21.5+5.5 cm、下降触发 15 cm | control/stepper.py、stepper.h、actions.c | 参数及动作核对；Debug 配置/编译 |

本次整理未新增比赛行为、修改依赖或执行实机动作。协议未变：双方命令编号、
载荷长度、大端字段、80 B 遥测、11 B 动作状态及 A 板 200 ms 失联急停保持。
当前部署状态统一在 README 的部署表；GitHub 发布不代表树莓派整库更新或 A 板烧录。
验证：55 文件语法、导入、6 项协议格式检查和 Debug 配置/编译通过；39 个本地文档
链接及锚点核对通过，待发布差异未包含树莓派密码。文档正文去重，历史调参和备份
记录保留；旧工作区中与本次无关的备份说明删除不纳入提交。

### 本地遥控工程同步

按用户指定更新 `备份/Uniforest_A_RC`：步进 400/60/400、Grap3 21.5/15/5.5 cm
与正式工程一致，保留 SBUS 通道、解锁、PB2/失联取消和状态 6 后等待完整结束。
既有动作/取消检查、源码一致性及 Debug 编译通过，未烧录或实机验证。
修改前文件备份在该工程 `build/before-sync-20260926-002431.zip`。
遥控副本按项目约定保留本地，不随正式上下位机发布；其记录见该工程 README。

## 2026-09-25

### Tag6 位置滤波改为 3 帧，平移 Ki、Kd 清零

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 位置中值窗口 5 → 3 帧，前后及横向 Ki、Kd → 0 | FirstTaskConfig，Task1 第二轮及 Task2 两轮继承 | 四套有效配置核对；语法、导入、协议及 Debug 编译；双方协议核对及 README |

适用两任务两轮 Tag6；当前关闭的 Tag3 共用这组参数，仍保持关闭。
前后 / 横向 Kp 保持约 2.66179 / 3.99269，航向 PID 保持 1.5/0.02/0.03。
最低平移 80 mm/s、各任务速度上限、加速度、距离及横向 ±8 mm、航向 ±3°、
单轴停车 / 两帧超差重启、四帧成功确认及超时故障处理保持。
协议未变：双方命令编号、载荷长度、大端字段、80 B 遥测、11 B 动作状态及
A 板 200 ms 失联急停均不变；无需修改固件或依赖。
测试入口为 `python main.py --task task1-r1`、`task1-r2`、`task2-r1`、`task2-r2`。
本次仅修改本地配置及文档，未同步树莓派、未执行实机动作；效果待现场确认。
验证：55 文件语法、导入、协议及 Debug 配置/编译通过；四套有效配置均为
3 帧滤波、平移 Ki/Kd=0，Kp、速度、加速度、容差及航向 PID 核对保持。
随后按用户要求定向同步树莓派，仅修改上述五个配置值及相关说明，保留远端其余差异。
远端 63 文件语法、导入和协议检查通过，四套完整配置对比确认仅上述五项变化；
串口和两路相机稳定路径存在，五份标定/数量检查配置保持原样，未启动实机动作。
远端原文件备份：`/home/uniforest/.uniforest-sync/tag6-p3-20260925-230113`。

### Grap3 回收 21.5 cm，回收到 15 cm 后触发下降

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 第 3 步水平回收 22 → 21.5 cm，下降触发 14 → 15 cm | actions.c 的 Grap3 D3 联动 | 脉冲换算与触发约束核对；Debug 编译；ACTIONS 及双方说明 |
| 第 5 步剩余回收 5 → 5.5 cm | Grap3 收尾并行段 | 总回收 21.5 + 5.5 = 27 cm，与伸出量一致 |

第 3 步仍先上升 9 cm，到 5 cm 时启动水平回收；完整上升结束且水平回收到
15 cm 后才下降 9 cm。收尾上升 5 cm 时复位前臂及其余舵机动作、等待、速度不变。
适用两轮 Task1 橙色抓取、补抓和 `python robot.py --action grap3` 单动作入口。
协议未变：动作 ID、命令编号、载荷长度、大端字段、80 B 遥测、11 B 动作状态及
A 板 200 ms 失联急停均保持，无上位机逻辑或依赖变化。仅修改正式工程，备份遥控
工程未修改；未同步树莓派、未烧录、未执行实机动作，须 CLion 烧录后现场确认。
验证：55 文件语法、导入、协议及 Debug 配置/编译通过；回收 8600 脉冲、
触发 6000 脉冲、剩余回收 2200 脉冲，总回收 10800 脉冲（27 cm）核对通过。

### 步进速度参数试调（按先后顺序汇总）

适用两任务两轮 Grap1/2/3、Build、补抓及默认独立步进；涉及 `stepper.h`、
`actions.c` 四种步进调用和 `control/stepper.py` 默认值。动作行程、交叉触发、
等待、S 曲线和 TIM7 算法不随这些调参改变。

| 日期 | 起步 / 巡航参数（μs）/ 加减速步数 | 软件计时与说明 |
| --- | --- | --- |
| 09-25 | 1000 / 70 / 400 | 用户由目标 6000 选择可实现的约 6250 脉冲/秒；加减速各 1 cm |
| 09-25 | 1000 / 60 / 400 | 巡航改为约 7143 脉冲/秒；相较 6250 提高约 14.3% |
| 09-25 | 1000 / 60 / 500 | 加减速各 1.25 cm；完整加速段约 0.640 秒 |
| 09-25 | 500 / 60 / 500 | 起步约 833 脉冲/秒；完整加速段约 0.340 秒 |
| 09-25 | **400 / 60 / 400（当前）** | 起步约 1042、巡航约 7143 脉冲/秒；加减速各 1 cm，完整加速段约 0.224 秒 |

TIM7 tick=10 μs；原巡航参数 83 经 6/5 缩放及取整为 10 tick 半周期，即
5000 脉冲/秒。70 取整为 8 tick、6250 脉冲/秒；60 取整为 7 tick、7142.86
脉冲/秒。当前起步 400 经缩放为 48 tick 半周期。位移换算为 400 脉冲/cm；
短行程仍将每段加减速长度限制为总步数的一半。时间为软件脉冲计算，不是机械实测。

每轮调参的 55 文件语法、导入、协议及 Debug 配置/编译均通过；当前双方默认值和
四种动作调用一致。协议未变：编号、载荷、大端字段、80 B 遥测、11 B 动作状态及
200 ms 失联急停不变；旧参数编码样例保留，无新依赖。

09-25 调参时仅修改正式工程，未同步树莓派独立步进默认值、未烧录或执行实机动作；
遥控副本于 09-26 另行同步。现场通过 CLion 烧录后，从 `python robot.py --action grap1` 对应的单动作入口小范围验证负载与失步情况，再测试其他 Grap/Build。

### Task1 投放顶墙按 180°更新航向基准

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 航向校准支持指定当前姿态对应的顺时针角度，默认仍为 0° | competition.py 的 _recalibrate_heading_zero | 正负 yaw、±180°边界与既有 0°调用核对 |
| 两轮 Task1 投放前向前顶墙后，开舱前按 180°修正零点 | _run_delivery_route，两轮共用 | README；后续回转目标仍为 0° |

零点按 wrap(当前 yaw + 参考顺时针角度) 计算，不改变陀螺仪原始读数。
Task1 起始顶墙、Task1/2 橙色短压墙、Task2 返回橙色区的前向顶墙仍按 0°校准；
其他顶墙不变。沿用顶墙超时降级成功的逻辑，超时接受后也更新基准。
协议未变：双方命令编号、载荷长度、大端字段、80 B 遥测、11 B 动作状态及
A 板 200 ms 失联急停保持，无固件或依赖改动，无需烧录。
已定向同步树莓派的航向校准函数及投放路线，保留远端其他策略差异。
远端语法、导入、协议与两轮 180°/0°换算及跨界检查通过，数量标定回读一致；
串口及两路相机设备路径存在。未执行实机动作，现场效果待确认。
备份：`/home/uniforest/.uniforest-sync/task1-heading-180-20260925-173756`。
验证：55 文件语法、导入、协议及 Debug 配置/编译通过；一次性角度换算检查覆盖
两轮 Task1、正负 yaw 与 ±180°边界，180°参考和默认 0°调用均通过。

## 2026-09-24

### 清理键盘遥控与重复临时入口

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 移除键盘遥控及底盘按键定距测试 | keyboard_control、chassis_distance_test、robot CLI | 清理 --keyboard、pynput 依赖及对应导入检查 |
| 移除重复动作、建筑单项、控制台包装及双视觉临时入口 | action_test、building_build_test、debug_console、vision_subsystems_test | 单动作统一 robot.py --action；更新两级 README 与下位机动作说明 |
| 清理已完成调参的一次性辅助程序 | .diagnostics 顶层部署、采样、修订与回退脚本 | 保留实拍图片、标定配置、日志和历史快照 |

比赛入口、动作实现、当前路线参数不改；舵机调零、数量检查标定、相机与视觉诊断、
VOFA 遥测及必要协议检查保留。协议未变：双方命令编号、载荷长度、大端字段、
80 B 遥测、11 B 动作状态和 A 板 200 ms 通信失联急停不变，无固件代码改动。
已同步树莓派：备份后移除 6 个入口，定向更新 6 个文件；远端语法、导入、
协议和 CLI 帮助检查通过，数量标定回读一致。保留远端既有代码与依赖差异；
远端 .diagnostics 顶层无待删脚本。串口及两路相机设备路径存在，未执行实机动作。
备份：`/home/uniforest/.uniforest-sync/cleanup-tools-20260924-213535`。
共移除 6 个重复/遥控入口及 `.diagnostics` 顶层 57 个一次性 Python 脚本。
验证：55 文件语法、模块导入、协议检查、Debug 配置与编译通过；比赛及单动作
入口帮助正常，现行源码无已删除模块引用，差异空白检查通过。

### 紫色对准改为 5 秒并超时抓取，合并部署橙色 5 秒参数

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 紫色对准独立上限 5 秒，到时接受当前位置并执行 Grap2 | Task2Config、_search_and_align_purple，两轮继承 | 参数与调用核对；README |
| 合并上一轮橙色粗对准 5 秒与超时跳过精对准 | FirstTaskConfig、两任务橙色入口 | 定向同步保留远端搜索实现 |

两轮 Task2 紫色原为 10 秒超时报错，现为 5 秒后先停底盘，返回抓取分支，
随后 Grap2 与 150 mm/s 短压墙按原流程并行。正常到位仍立即抓取，不等满 5 秒。
目标持续丢失 0.5 秒仍返回搜索；紫色搜索距离耗尽仍跳过 Grap2，通信/取消和
机构异常仍中止。橙色精对准保持 0.5 秒，转向、Tag6 和建筑对准上限不变。
协议未变：命令编号、载荷长度、大端字段、80 B 遥测、11 B 动作状态及 A 板
200 ms 通信失联急停保持；无新依赖或下位机改动，无需烧录。现场效果待确认。
验证与部署：本地 61 文件语法、导入、协议及 Debug 配置/编译通过；四份策略/文档
已定向上传并回读一致，远端 69 文件检查与两任务两轮 5 秒配置/调用检查通过。
串口和两路相机设备路径存在，数量检查参数及标定不变，未执行实机动作。
备份：`/home/uniforest/.uniforest-sync/cube-align-5s-20260924-210325`。

### 橙色粗对准上限从 10 秒改为 5 秒

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 新增独立橙色粗对准上限 5 秒，两任务显式传入 | FirstTaskConfig、Task1 _align_orange、Task2 _grab_task2_orange | 两轮继承配置/调用核对；两级 README |

两轮 Task1/Task2 及补抓均适用。超时仍停车、跳过精对准并直接抓取；
紫色共用默认上限保留 10 秒，橙色精对准 0.5 秒、目标丢失 0.5 秒及搜索预算不变。
协议未变：命令编号、载荷长度与字节序、80 B 遥测、11 B 动作状态和 A 板
200 ms 失联急停保持，无新依赖或固件改动。本次仅本地修改，尚未同步树莓派；
实机效果待用户确认，不执行实机动作。
验证：61 文件语法、导入、协议及 Debug 配置/编译通过；核对两任务两轮配置
均为橙色粗对准 5 秒、紫色 10 秒，两个橙色入口显式使用新参数。

### 常规转向超时余量改为最多额外 1.5 秒

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 固定 5000 ms 改为转角/速度估算时间＋加速/保持时间＋1500 ms | control/chassis.py 的 turn | 双向转角、90°/180°上限及超时零速检查；两级 README |

用户要求将超时改为最多额外等待 1.5 秒。总上限为
`abs(target_deg) / speed_deg_s * 1000 + TURN_ACCEL_MS + max(0, hold_ms) + 1500` ms。
当前路线 120°/s、加速 600 ms、保持 0 ms：90° 为 2850 ms，180° 为 3600 ms；
不是固定再等待 1.5 秒，到位仍立即结束。超时按降级成功继续，日志同时记录耗时和上限。
最低 8°/s、±1.5° 容差、超调反向、通信/遥测/急停/机构故障保护不变。
适用两任务两轮常规路线与 Build 后转向。协议未变：双方命令编号、载荷长度、
大端字段、80 B 遥测、11 B 动作状态与 A 板 200 ms 失联急停保持；无新依赖、无需烧录。
参数由用户指定，现场效果待复测；助手不执行实机动作。
验证与部署：本地 61 文件语法、导入、协议及 Debug 配置/编译通过；一次性软件
时序检查核对正负 90°、180°、显式保持和提前到位。底盘代码与两份文档已定向
上传并回读一致，远端 69 文件语法、导入、协议与两任务两轮参数检查通过。
串口及两路相机设备路径存在，计数标定不变，未执行实机动作。
备份：`/home/uniforest/.uniforest-sync/turn-timeout-margin-20260924-201034`。

### 常规转向末段最低速度与超时降级

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 容差外最低期望转速 8°/s，进入 ±1.5° 即零速 | control/chassis.py 的 turn | 顺逆转向、越界、到位与软件时序边界检查；README |
| 前馈按当前误差方向，越过目标清除积分；起步同时受剩余距离约束 | 同上 | 保留 120°/s、600 ms 加速、40° 减速区及 PID 参数 |
| 健康通信下 5 秒超时先停车再告警，降级返回并继续路线 | 同上及两任务共用调用 | 普通/并行转向均适用；日志含已转/剩余角度和末次输出 |

用户现场反馈：Task2 紫色 Grap2 后退 100 mm 完成，转回 0° 时接近到位后停住，
随后出现 `turn timed out during action overlap`。用户要求增加末段最低转速，
并将转向超时按降级成功处理；8°/s 为待现场验证的软件参数，不是实测克服阻力值。
仅健康的转向到时允许降级；失联、遥测缺失/超过 0.5 秒、急停代次变化、发送失败
及并行机构故障仍急停并抛出异常。计时改用单调时钟，避免系统时间调整误触发超时。
正常结束与降级结束仅发底盘零速，不发整车急停，避免取消并行 Grap2/Build 或关闭吸气。
适用两轮 Task1/Task2 的常规路线转向及 Build 后相对转向；Tag6 独立视觉控制不变。
协议未变：双方 0x10/8 B 的四路 int16 大端转速、80 B 遥测、0x83/11 B 动作状态、
0x02 急停及 A 板 200 ms 通信失联保护保持。无新依赖、无下位机改动，不需烧录。
验证：本地 61 文件语法、导入、协议及 Debug 配置/编译通过。12 项一次性软件
边界回放核对顺逆转、最低输出、越过目标反向、跨越 ±180°、容差内零速、普通/
并行超时降级，以及失联/急停/遥测陈旧/发送失败/机构故障中止；不代表机械效果。
部署：底盘控制和两份文档已定向上传并回读一致，远端 69 文件语法、导入、协议
检查通过；核对两任务两轮 120°/s、8°/s、±1.5° 和 5 秒，计数标定保持不变。
串口及两路相机设备路径存在；未执行实机动作、未烧录固件。
备份：`/home/uniforest/.uniforest-sync/turn-finish-20260924-195501`。

### 橙色粗对准超时直接抓取与文档整理

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 粗对准达到 10 秒上限后接受当前位置并跳过精对准 | competition.py 的 _align_orange/_align_cube，task2.py 的 _grab_task2_orange | 两轮继承、正常成功/超时/目标丢失分支核对；两级 README |
| 汇总 Tag6、Grap3、吸气关阀与数量检查最新状态 | 根目录及上下位机文档 | 区分当前参数、历史记录、树莓派部署与固件烧录 |

用户指定：Task1 使用 Grap3、Task2 使用 Grap1，仍与短压墙同时启动。正常粗对准
成功仍进入原精对准；持续丢失目标 0.5 秒返回搜索，通信/取消等异常不转为成功。
仅橙色粗对准调用开启超时放行，紫色、Tag6 与建筑对准行为保持。
协议未变：双方命令编号、载荷长度及大端字段、80 B 完整遥测、11 B 动作状态、
动作启动 0x50/6 B 和 A 板 200 ms 失联急停保持，无新增依赖或固件改动。
适用两轮 Task1/Task2 比赛入口；已定向同步树莓派，未执行实机动作，效果待确认。
验证：61 文件 Python 语法、导入、协议格式检查和 Debug 配置/编译通过。
文档统一记录当前 Tag6 80 mm/s、±8 mm 和检查 200/300/200 ms，保留历史调参记录。
部署：2026-09-24 上传两份策略与 README/CHANGELOG，逐一回读一致，保留远端
搜索实现。69 文件语法、导入、协议检查通过，两任务两轮超时放行/跳过精对准
入口已核对；10 秒粗对准上限、0.5 秒目标丢失与数量检查 200/300/200 ms 保持。
串口及两路相机设备路径存在，计数标定未改；未打开设备执行动作或烧录。
备份：`/home/uniforest/.uniforest-sync/orange-coarse-timeout-20260924-193904`。

### 数量检查缩短等待，复位与后续后退重叠

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 翻转 37.2° 后等待 200 ms，前臂 120° 后等待 300 ms | control/carried_cube_inspection.py、独立 carried_cube_count_test.py | README、检查说明；语法、导入和独立 dry-run |
| 3/null 在前臂复位 ACK 后开始后退，同时完成 200 ms 等待与翻转复位 | robot.py、两任务 Strategy、检查模块 | 两轮共用；只执行一次后退，先冻结横移补偿测量 |
| 0/1/2 复位后再搜索；距离耗尽跳过检查仍后退一次 | 同上 | 分支与取消/通信异常保护核对 |

适用两轮 Task1/Task2 和独立检查入口；识别丢弃 3 帧、采集 8 帧、下方 30% 橙色
宽度和紫色计 1 保持。复位仍为前臂逻辑 90°、等待 200 ms、翻转 97.2°；
通过现有底盘 monitor_action 在控制循环内推进复位，不创建脱离任务的后台线程。
后退完成但复位未完成时先补齐复位，再继续路线；相机异常不作为 null 放行。
独立入口没有底盘路线，完整复位后返回。用户指定新参数，实机效果待确认。
协议未变：双方 0x20 整数 2 B/小数 3 B 大端角度、0x10 的 8 B 大端四轮转速、
0x51 空载荷查询、0x83 的 11 B 动作状态、80 B 完整遥测及 200 ms 失联急停保持。
无新依赖，无本次下位机改动；已于 2026-09-24 按用户要求上传树莓派，未执行实机动作。
验证：61 文件 Python 语法、导入、协议格式和 Debug 配置/编译通过，独立入口
dry-run 确认 200/300/200 ms。一次性无硬件控制边界检查覆盖 3/null 重叠、0/1/2
不提前搜索、独立入口同步复位，以及失联/取消/复位 ACK 失败的急停和锁释放。
此检查只验证软件时序与异常分支，不代表舵机到位或视觉效果已通过实机验证。
部署：8 个代码/文档文件定向上传并逐一回读一致，保留远端橙色搜索实现和
quiet_heartbeat 兼容参数。远端 69 文件语法、导入、协议格式及独立 dry-run 通过，
两任务两轮复位重叠入口、后退距离已核对。串口和两路相机稳定设备路径存在，
计数标定文件保持一致；未打开相机取帧、发送串口指令或烧录固件。
上传前备份：`/home/uniforest/.uniforest-sync/count-restore-overlap-20260924-174010`。

### Grap3 收尾上升到 5 cm 时提前复位前臂

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 第 5 步同时上升 9 cm、水平回收 5 cm，本段上升到 5 cm 时前臂复位 90° | actions.c 的 grap3 收尾段，复用 DUAL_ASYNC/WAIT_PROGRESS/JOIN | 阈值与 JOIN 调用链核对；ACTIONS、上位机 README |

适用两轮 Task1 的 Grap3 及 `action_test.py grap3` 独立入口。阈值相对本段起点，
由主循环读取脉冲进度后下发舵机指令；不等待舵机到位，两轴完成后再执行 HOME。
无新增固定等待；Grap1/2、Build、短压墙及 Grap3 完整结束后再继续底盘的逻辑不变。
前臂逻辑 90° 经 +12° 偏置输出 102°。协议未变：双方命令编号及载荷长度、
字节序、80 B 遥测、11 B 动作状态及 200 ms 失联急停保持，无依赖变动。
需经 CLion 的 OpenOCD + DAPLink 烧录 A 板后生效，现场效果待复测，不执行实机动作。
验证：61 文件 Python 语法、导入、协议检查及 Debug 配置/编译通过；源码差异
仅在 Grap3 最后一组步进中加入进度触发前臂指令，JOIN 仍保证两轴完成后 HOME。

### Task2-R2 最后一块吸不住：修复吸气切换漏发关阀 PWM

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 吸气启动时明确写入释放阀关闭 PWM，再开启气泵 | A 板 suction.c 的 Suction_PumpOn | 释放/吸气/计时调用链、Debug 编译；双 README 与 ACTIONS |

用户确认 Task2 第二轮 Grap1 在吸盘接触后未吸住，抬起时方块留在原地，补抓正常。
正常最后一次抓取与补抓调用同一个 Grap1；第二轮没有单独的末次抓取吸气参数，
数量检查也在 ACTION_DONE 之后才启动。确定的软件缺陷是：Release 打开 CH2 并设置
1000 ms 关阀计时；若此前调用 PumpOn，旧函数清除计时和 g_valve_on，却不写 CH2，
导致阀保持打开，后续 Update 也不会关阀。新实现补齐实际关闭输出。
有紫色时 Task2 先抓两个橙色，第二个即最后一个，紧接上一轮释放；补抓前的数量
检查含 300+500+200 ms 等待及取帧时间，允许上一次释放的关阀计时完成。
这是与现象高度吻合的条件性解释，尚无现场时序记录证明失败当次间隔小于 1 秒，
也未确认板上固件版本；不将编译通过视为实机根因已经验证。
所有吸气入口共用此修复，不修改 Grap 行程/等待、视觉参数或比赛路线。
协议未变：0x23 吸盘命令、0x50/0x51 动作接口、载荷、字节序、80 B 遥测、
11 B 动作状态及 200 ms 失联急停保持；无新依赖。需用户通过 CLion 烧录 A 板，
先小范围确认快速释放后再吸气能关闭释放阀，再复测 Task2-R2，助手不启动实机动作。
验证：61 文件 Python 语法、导入、协议检查及 Debug 配置/编译通过；源码差异
仅补充 PumpOn 的 CH2 关闭输出及注释，释放计时和急停路径保持。尚未烧录。

后续现场反馈：2026-09-24 用户确认“确实是这个问题”，故障原因确认为上述
吸气切换时漏发释放阀关闭 PWM。此结论来自用户现场反馈；未据此推断具体烧录
版本、复测次数或完整比赛通过情况。

### 回退 Grap1/2 前臂阈值动作修改

按用户要求撤销本轮新增的前臂 95°/90° 阈值指令，Grap1/2 的下降和上升均恢复
18.5 cm；Grap2 上升到 5 cm 的底盘就绪通知保持。代码及当前动作文档恢复，
此前 Tag6 参数、单轴停车、路线调整和前臂 +12° 偏置保留。协议未变。
该次动作修改未由助手烧录或执行实机动作。
回退后 61 文件语法、导入、协议检查及 Debug 配置/编译通过。

### 两任务两轮 Tag6 最低平移速度统一回调至 80 mm/s

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| Tag6 前后、横向非零期望速度下限 100→80 mm/s | FirstTaskConfig、Task2Config，第二轮继承 | 四套配置核对；项目及上位机 README |

距离/横向容差仍为 ±8 mm，航向 ±3°、最低转速 8°/s；单轴合格立即零速、
两个新帧超差后重启、取消精对准、最终 4 帧确认、PID 和滤波保持。Task2 的
共用标签最低速度也影响以后恢复的 Tag3，当前仍跳过。入口为两轮 Task1/Task2
或完整比赛，现场效果待复测。协议未变：命令编号、载荷长度、字节序、80 B
遥测、11 B 动作状态及 200 ms 失联急停不变，无固件或依赖修改。
验证与部署：本地 61 文件语法、导入、协议及 Debug 配置/编译通过，四套配置
核对通过。四文件已上传树莓派并回读一致，远端 69 文件语法、导入、协议与
两任务两轮 80 mm/s、±8 mm 配置及单轴停车调用检查通过。备份位于
`/home/uniforest/.uniforest-sync/tag6-80-8-20260924-153209`；未实机运动或烧录。

### Task2 紫色抓取前靠墙前进改为 250 mm

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 转到 -90° 后、向前顶墙前的前进距离 300→250 mm | Task2Config.wall_premove_mm，第二轮继承 | 两轮配置核对；项目与上位机 README |

两轮均保持前进 400 mm/s、300 ms 加速，随后 300 mm/s 向前顶墙；紫色搜索和
其他路线保持。入口为两轮 Task2 或完整比赛，参数由用户指定，现场效果待复测。
协议未变：命令编号及载荷、0x10 / 8 B 大端转速、80 B 遥测、11 B 动作状态
及 200 ms 通信失联急停保持。无固件或依赖变化，不自动执行实机动作。
验证与部署：本地 61 文件语法、导入、协议及 Debug 配置/编译通过，两轮配置
核对通过。三文件已定向上传树莓派并回读一致，远端 69 文件语法、导入、协议及
250 mm 路线、100 mm/s / ±8 mm Tag6 参数检查通过。原文件备份于
`/home/uniforest/.uniforest-sync/purple-premove-250-20260924-151827`，未执行实机动作。

### 两任务两轮 Tag6 统一最低平移 100 mm/s、距离与横向容差 ±8 mm

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| Task1/Task2 Tag6 最低非零平移期望速度 50/80→100 mm/s | FirstTaskConfig、Task2Config，第二轮继承 | 四套配置核对；项目与上位机 README |
| Tag6 距离和横向容差统一 ±8 mm | Task1 delivery_tag 容差及 Task2 build_tag 容差 | 保持 425 mm、横向 0 mm 目标和航向 ±3° |

单轴合格立即零速、两个有效新帧超差后重启、取消精对准和三项合格连续 4 帧确认
保持，PID、滤波、速度上限及加速度不变。当前跳过的 Tag3 容差仍为 ±10 mm，
但其共用 Task2 标签最低平移速度也变为 100 mm/s。入口为两轮 Task1/Task2 或
完整比赛；参数由用户指定，现场效果待复测。协议未变：双端命令编号、载荷长度、
大端数据字段、80 B 遥测、11 B 动作状态及 200 ms 通信失联急停保持。
无固件或依赖变化，不启动实机动作。
验证与部署：本地 61 文件语法、导入、协议检查及 Debug 配置/编译通过，四套
配置核对通过。四文件定向上传树莓派并回读一致，远端 69 文件语法、导入、
协议和两任务两轮 100 mm/s、±8 mm 配置及单轴停车调用检查通过。
原文件备份于 `/home/uniforest/.uniforest-sync/tag6-100-8-20260924-151322`，未实机运动或烧录。

### Task1/Task2 Tag6 单轴合格即停车，两个新帧超差后重启

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 单轴合格直接零速，绕过软件减速并清零 PID/速度状态 | competition.py 标签控制函数，Task1/Task2 Tag6 显式启用 | 控制分支检查；上位机 README |
| 单轴停车后连续两个有效新帧超差才恢复运动 | tag_controller.py 的 AxisToleranceHold | 中断、重复帧、独立轴及最终确认边界检查 |

适用两轮 Task1/Task2 Tag6，入口为对应单任务或完整比赛。Task2 前后/横向容差
±10 mm，Task1 保留约 ±9.02/±7.51 mm，航向均为 ±3°；最低平移期望
Task1 50 mm/s、Task2 80 mm/s，转向最低 8°/s，重启仍经过加速限制。
PID、5 帧位置中值滤波、三项合格 4 个新帧确认及故障中止保持。丢失/跳变清除
超差计数，重复帧不累计；最终通过仍检查真实容差，不以停车状态替代。
Task1 也取消精对准，三项合格后同样停车确认 4 个新帧；当前关闭的 Tag3
不启用此单轴策略。协议未变：双方命令编号及载荷、
0x10 / 8 B 大端转速、80 B 遥测、11 B 动作状态和 200 ms 失联急停保持。
无固件或依赖变化。软件零速不代表机械瞬间停止，现场效果待复测。

验证与部署：本地 61 文件语法、导入、协议检查及 Debug 配置/编译通过。
对实际控制函数进行帧边界检查：单轴立即零速、重复帧不计数、丢帧清除计数、
两帧后从零速重新加速及最终四帧确认通过；检查隔离滤波，正式配置仍为 5 帧，
不据此宣称机械效果通过。两任务两轮调用均已核对。
五文件已定向上传树莓派并回读一致，远端 69 文件语法、导入、协议及调用/配置
检查通过。原文件备份于 `/home/uniforest/.uniforest-sync/tag6-axis-stop-20260924-145909`。
未启动机器人或烧录固件。

### 文档基线与部署状态整理

| 行为 / 状态核对 | 受影响模块 | 文档与验证 |
| --- | --- | --- |
| Task2 路线、Tag6 ±10 mm/无精对准/最低 80 mm/s、常规转向 120°/s | competition.py、task2.py、chassis.py | 根目录与上位机 README，核对远端配置 |
| Grap1/2 垂直行程 18.5 cm、前臂偏置 +12° | actions.c、servo.h/servo.c | ACTIONS、下位机 README/PROJECT、调零说明；区分上传与烧录 |
| 清理过期部署说明，保留历史验证记录与远端独立内容 | 项目文档 | 以当前源码和实际上传记录为准，不将配置写成实测结论 |

本次整理不改变程序行为、依赖或协议。协议未变：双端命令编号、载荷长度、
大端数据字段、80 B 完整遥测、11 B 动作状态与 200 ms 失联急停保持。
上位机参数已于 9 月 23 日定向上传；本次上传文档，不覆盖远端旧搜索及兼容接口。
最新固件的前臂偏置与 Grap1/2 行程仍需用户在 CLion 烧录并小范围复测。
验证：本地 61 文件 Python 语法、导入、协议检查及 Debug 配置/编译通过；
文档相对链接检查通过。树莓派 SSH 连续两次连接超时，本次文档尚未上传。

### GitHub 提交前核对

将上述文档基线与近期代码一并归档：前臂调零入口和 +12° 偏置、心跳静默、
方块对准提速及旧帧失效处理、Task2 两轮路线、Tag6 容差和最低速度、
常规转向前馈 120°/s、Grap1/2 垂直行程 18.5 cm。
再次运行本地 61 文件语法、导入、6 项协议格式检查及 Debug 配置/编译均通过；
文档相对链接和待提交文件凭据检查通过。诊断快照、密码、虚拟环境和构建产物
不纳入提交。此次 GitHub 同步不改变树莓派部署状态，也不代表已烧录固件。

## 2026-09-23

### 常规转向前馈提高至 120°/s，与 Tag6 最低平移速度一起同步

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 两轮 Task1/Task2 常规路线转向前馈 90→120°/s | FirstTaskConfig.delivery_turn_speed_deg_s，路线转向调用 | 两轮配置核对、上位机 README |
| 同步上一轮 Task2 标签最低平移速度 80 mm/s | Task2Config.delivery_tag_min_linear_mm_s | 两轮 Tag6 容差与非精对准调用保持 |

现有 chassis.turn 按速度比例缩放：减速区 30→40°，PID 修正限幅 ±60→±80°/s；
前馈并非最终速度硬上限。起步加速 600 ms、到位容差 ±1.5°、比赛额外保持 0 ms
不变，视觉对准转向参数不变。协议未变：命令编号、底盘 0x10 / 8 B 大端转速、
80 B 遥测及 200 ms 通信失联急停保持，无固件或依赖改动。
入口为两轮 Task1/Task2 或完整比赛，参数由用户指定，现场效果待复测。
验证与部署：本地 61 文件语法、导入、协议及 Debug 配置/编译通过；四文件
定向同步树莓派并回读一致。远端 69 文件语法、导入、协议、两轮路线和速度配置
核对通过：常规转向 120°/s、Task2 标签最低平移 80 mm/s，Tag6 ±10 mm 且无精对准。
原文件备份于 `/home/uniforest/.uniforest-sync/turn-tag6-speed-20260923-235613`。
未执行实机动作或烧录 A 板；此前 Grap1/2 的 18.5 cm 行程仍需烧录固件后生效。

### Task2 Tag6 平移最低纠偏速度提高至 80 mm/s

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 前后、横向非零期望速度下限 50→80 mm/s | Task2Config.delivery_tag_min_linear_mm_s，第二轮继承 | 两轮配置核对；上位机 README |

Task1 保持 50 mm/s；Task2 转向最低 8°/s、平移加减速 300 mm/s²、
±10 mm 容差及取消精对准保持。加减速限制在最低限幅之后，起步/停车时可低于
80 mm/s，进入容差允许零速。该字段为 Task2 标签共用配置，若以后恢复 Tag3
也使用 80 mm/s；当前 Tag3 跳过。协议未变，无固件或依赖调整。
适用两轮 Task2 或完整比赛入口，参数由用户指定，现场效果待复测。
验证：61 文件语法、导入、协议检查及 Debug 配置/编译通过；两轮最低平移速度
80 mm/s、转向 8°/s 和 Task1 保持 50 mm/s 均已核对。仅本地修改，尚未上传。

### Grap1 / Grap2 垂直行程统一改为 18.5 cm

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| Grap1、Grap2 的下降和回程上升由 18→18.5 cm | A 板 actions.c 的两套动作表，共四处距离 | Debug 编译、ACTIONS 和上位机 README |

适用两轮 Task2 的橙色 Grap1、紫色 Grap2 及对应独立动作入口。
水平行程 22/27 cm、下降触发点 5/10 cm、回程水平触发及 Grap2 底盘就绪阈值
5 cm 均保持；不新增等待，Grap3 和 Build 不变。每段按 400 步/cm 转为 7400 步。
协议未变，无上位机逻辑或依赖调整；必须通过 CLion 的 OpenOCD + DAPLink
烧录 A 板后生效。距离为用户指定的软件参数，现场效果待复测，未执行实机动作。
验证：61 文件 Python 语法、导入、协议检查及 Debug 配置/编译通过；
动作源码差异仅为上述四处 18→18.5 cm，尚未烧录。

### Task2 Tag6 容差改为 ±10 mm，取消精对准

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 距离容差约 ±9.02→±10 mm，横向约 ±7.51→±10 mm | Task2Config 的 build_tag 两项容差，第二轮继承 | 两轮配置核对、上位机 README |
| Tag6 不再进行精对准，进入容差停车并确认连续 4 个新帧 | Task2 的 _align_delivery_tag 调用传入 fine_align_enabled=False | 复用已有非精对准路径，核对调用及默认模式 |

仅适用两轮 Task2；Tag6 目标保持距离 425 mm、横向 0 mm、航向 180°±3°。
速度、加速度、滤波及超时参数保持，Task1 Tag6 不变。协议未变：双端命令编号、
0x10 / 8 B 大端四轮转速、80 B 遥测及 A 板 200 ms 失联急停不变；无固件或依赖修改。
入口为两轮 Task2 或完整比赛；参数由用户指定，现场效果待复测，不自动执行实机动作。
验证：61 文件语法、导入、协议检查及 Debug 配置/编译通过；两轮配置及 Tag6
调用关闭精对准已核对，Task1 容差未变。本次仅修改本地代码与文档，尚未上传树莓派。

后续部署：用户要求上传后，已定向同步 Task2 代码及两份文档并回读一致。
树莓派 69 文件语法、导入、协议与两轮路线/Tag6 参数检查通过，Task1 保持原参数。
原文件备份于 `/home/uniforest/.uniforest-sync/task2-tag6-20260923-233208`。
未启动机器人或烧录固件。

### Task2 退出挡墙及第二段坡道距离调整

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 橙色抓取后后退 500→100 mm | Task2Config.post_orange_reverse_mm，两轮共用 | 保持 400 mm/s；上位机及项目 README |
| 横向补偿基准 700→550 mm，仍减去编码器实测净右移量 | post_orange_lateral_base_mm 及既有补偿调用 | 正右负左、零值跳过，保持 400 mm/s |
| 转到 180° 后第二段坡道前进 2100→2800 mm | build_route_distance_mm，两轮共用 | 保持 800 mm/s、显式加速 800 ms |

适用第一、第二轮 Task2，入口为两轮 Task2 或完整比赛。参数由用户指定，
现场效果待复测；本次不启动机器人。协议未变：双方命令编号、0x10 / 8 B
大端四轮转速、80 B 遥测布局及 A 板 200 ms 失联急停保持；无固件或依赖改动。

验证：61 文件 Python 语法、导入、协议检查及两轮配置核对通过。Debug 配置通过，
编译因现有 `Uniforest_A/Core/Src/main.c:313` 行末额外的 `·` 字符失败；
该文件已有用户改动，本次保留未改。此次仅更新本地代码与文档，未上传或实机测试。

后续修复：用户要求解决编译问题并上传，已删除 main.c:313 行末误输入的 `·`；
时钟参数保持不变。重新运行 61 文件语法、导入、协议检查及 Debug 配置/编译均通过。
Task2 代码及两份文档已定向同步树莓派并回读一致，保留远端其他既有实现。
远端 69 文件语法、导入、协议和两轮路线参数检查通过；备份位于
`/home/uniforest/.uniforest-sync/task2-exit-route-20260923-231526`。
未运行实机动作或烧录 A 板固件。

### 前臂默认输出修正为 102°，固定偏置 +12°

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 用户将原逻辑 90° 对应的新默认输出由 101° 修正为 102°；偏置 +11→+12° | A 板 servo.h 的 SERVO_ARM_FRONT_OFFSET_TENTH=120，既有 servo.c 统一输出层 | 固件编译；双端 README、ACTIONS、调零说明同步 |

适用两轮全部前臂动作：逻辑 90/95/120/0° 对应输出 102/107/132/12°。
上位机指令与动作表保留原逻辑角度，其他舵机不变，协议未变。
实机入口为调零工具或 Grap/Build、数量检查；本次参数由用户指定，现场效果待复测。
须由用户通过 CLion 的 OpenOCD + DAPLink 烧录后生效，本次不执行实机动作。
验证：61 文件 Python 语法、导入、协议检查及固件 Debug 配置/编译通过；尚未烧录。


### Task2 起步路线调整，暂时跳过 Tag3 及横移

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 第一轮起步 2350→2500 mm，第二轮显式保持 2350 mm | Task2Config、Task2Round2Config | 两轮配置核对，速度 800 mm/s、加速 800 ms 保持 |
| 保留转到 -90°，暂时跳过 Tag3 对准及第一轮原右移 100 mm | _run_partial_task 的 tag3_alignment_enabled 开关 | 用户明确确认一并取消横移；Tag6 不受影响 |
| 两轮靠墙前定距前进 250→300 mm | wall_premove_mm | 400 mm/s、300 ms 加速；随后 300 mm/s 向前顶墙 |

新流程：第一轮前进 2500 mm / 第二轮 2350 mm → 转到 -90° → 前进 300 mm → 顶墙。
`tag3_alignment_enabled=False` 默认关闭两轮的 Tag3 对准及其后横移，原 Tag3 参数
留存以便恢复；其余紫色/橙色抓取、数量检查、第二段坡道、Tag6 和 Build 路线保持。
协议未变：命令编号、0x10 / 8 B 大端转速、80 B 遥测及 200 ms 失联急停保持。
无需为本次上位机路线变化烧录，无新依赖。入口为两轮 Task2 或完整比赛；
路线距离是用户指定的软件参数，现场效果待用户复测，不自动启动机器人。

验证与部署：本地 61 文件语法、导入、协议检查及 Debug 配置/编译通过。
路线代码和两份文档已同步树莓派并回读一致；远端 69 文件语法、导入、协议及
两轮路线参数检查通过（2500/2350 mm，靠墙前进 300 mm，跳过 Tag3 与横移）。
旧文件备份于 `/home/uniforest/.uniforest-sync/task2-route-20260923-224124`。
未启动机器人或烧录固件，现场路线效果待复测。

### Tag3 容差统一 ±10 mm，取消精对准

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 两轮 Task2 的 Tag3 距离、横向容差均设为 ±10 mm | Task2Config 及第二轮继承 | 核对目标仍为 250 mm、X=0、航向 -90°±3° |
| Tag3 各轴进入合格容差后不再追求精对准死区，三项合格后停车，连续 4 个新帧确认即继续 | _align_delivery_tag 增加默认开启的精对准开关；Tag3 单独关闭 | 核对确认重置、丢帧处理及 Tag6 默认精对准路径 |

取消 Tag3 原最长 1 s 精对准以及 ±1.5 mm / ±0.5° 的精度要求，保留原速度、
加速度、5 帧中值滤波、50 ms 周期、12 s 总超时和 2 s 丢失超时。
两轮 Tag6 仍显式传入原目标与容差，保留精对准；其他方块/建筑对准不变。
协议未变：双端底盘 0x10 / 8 B、大端四轮转速、80 B 遥测、11 B 动作状态及
200 ms 失联急停保持。无新依赖，无需为本次策略变化烧录固件。
实机入口为两轮 Task2 或完整比赛；现场效果待用户确认，不自动执行运动。

验证与部署：本地 61 文件语法、导入、协议格式检查及 Debug 配置/编译通过；
离线控制检查确认无精对准模式在容差内停车确认，默认精对准路径仍保留。
四文件已定向同步树莓派并回读一致，远端 69 文件语法、导入、协议及两轮配置
检查通过，Tag6 容差及默认精对准未改。旧文件备份于
`/home/uniforest/.uniforest-sync/tag3-alignment-20260923-220022`。未启动机器人或烧录。

### 方块对准提速与视觉失效处理

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 起步 40→80 mm/s，加减速限制 300→800 mm/s²，远处上限 420→500 mm/s，降速距离 180→120 mm | competition.py 共用配置，两轮 Task1/Task2 继承 | 配置及速度计算核对，上位机 README |
| 确认 3→2 个新帧；橙色微调 ±3→±5 mm，最长 1→0.5 s | vision_targets.py、共用对准 | 紫色保持原 ±5 mm 窗口且不执行独立微调 |
| 重复旧帧到期停车，丢失后清零速度与旧目标，恢复时重新起步；微调不得用过期位置放行 | _align_cube、_fine_align_orange | 仅检查控制状态与时间边界，不以离线结果宣称实机效果 |

适用两轮 Task1/Task2 方块对准。Kp=1.5、基础限幅 250 mm/s、近处最低期望
100 mm/s、搜索 300 mm/s、控制间隔 30 ms、粗窗口和边缘保持参数均不变。
协议未变：双方底盘 0x10 / 8 B、四路 int16 大端目标转速、80 B 遥测布局、
11 B 动作状态及 A 板 200 ms 失联急停保持。此次无固件或依赖变化。
实机入口为 Task1/Task2 两轮或完整比赛；用户说明树莓派未连接，因此仅本地修改，
不尝试上传或实机动作，耗时和抓取成功率待现场确认。

验证：本地 61 文件语法、导入、现有协议格式检查及 Debug 配置/编译通过。
对实际控制函数进行离线状态检查：重复帧不重复确认、冻结帧到期停车、短暂丢失后
从 80 mm/s 重新起步、拒绝过期微调兜底且保留新鲜粗窗口兜底；两轮配置继承通过。
检查未访问串口或相机，不代表实车惯性、抓取容错和提速效果已验证。

后续部署：用户随后要求上传，2026-09-23 已定向同步 competition.py、vision_targets.py
及远端参数文档，保留树莓派既有搜索实现与其他文档内容。四文件回读一致，远端
69 文件语法、导入、协议检查通过，两轮 Task1/Task2 的新参数均已核对。
旧文件备份于 `/home/uniforest/.uniforest-sync/cube-align-20260923-212053`。
未发送串口指令、启动比赛或烧录固件，现场提速效果待用户验证。

### 前臂启用 +11° 固定偏置，正常心跳静默

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 用户确认原 90° 对应新舵机 101°，偏置设置为 +11° | A 板 servo.h 的 OFFSET_TENTH=110、既有统一 servo.c 输出层 | 核对两轮 Grap/Build、复位及数量检查；Debug 编译 |
| 正常 PONG 不打印，只在发送失败或超过 200 ms 无响应时提示，同一异常只提示一次 | robot.py 心跳线程与回调 | 语法/导入/协议检查；心跳发送及故障保护保持 |
| 调零提示不再假定板上偏置为零 | tools/arm_zero_adjust.py、操作说明、双 README | 区分逻辑角度、已烧录偏置和增量标定 |

适用两轮全部前臂动作，仍使用原逻辑角度：90→101、95→106、120→131、0→11°。
现有动作前臂逻辑范围 0～120°，补偿输出 11～131°，未触及 0～180° 限幅。
默认姿态由用户实机确认；完整动作效果及新固件烧录状态待确认。测试入口为独立
调零工具和 Grap/Build；不自动执行动作或烧录。偏置必须通过 CLion 烧录后生效。
协议未变：双方舵机命令编号/载荷/字节序、动作状态 11 B、遥测 80 B 及 A 板
200 ms 失联急停保持；50 ms 心跳和现有 150 ms 调零检查保持，无新依赖。

验证与部署：本地 61 文件语法、导入、协议检查及 Debug 编译通过，正常 PONG
回调静默且仍更新握手事件和接收时间。树莓派定向替换 `robot.py` 的两个心跳方法，
保留其他远端差异，并同步调零工具和说明；三文件回读一致，远端 69 文件语法、
导入及协议检查通过。备份在 `/home/uniforest/.uniforest-sync/arm-offset-20260923-165635`。
未发送串口控制命令或烧录固件；日志修改下次启动程序生效。

### 修复前臂调零启动后立即退出

用户日志显示提示输入后、尚未输入角度即被链路检查中止。源码存在启动竞态：
`Robot.start()` 等待 100 ms，首个周期心跳再等待 50 ms，距连接 PONG 可超过
150 ms；原工具在此之前固定链路代次，首个周期 PONG 更新代次后即被判为故障。
修复限于 `tools/arm_zero_adjust.py`：等待启动后的新遥测/PONG 连续稳定 300 ms
后再固定代次；开始调零后的 150 ms 检查、重启/急停检测与故障退出保持。
新增 `--check-link` 无舵机指令通信预检，并细分错误原因，便于区分真实断线。
受影响范围仅独立调零工具与操作说明，无新依赖。协议未变：舵机命令编号、
载荷、大端字段、80 B 遥测及下位机 200 ms 失联急停均未改，不需烧录本次修复。
入口为原调零命令或 `--check-link`；默认角度及偏置仍待用户确认。

验证与部署：本地 61 文件语法、导入、协议检查及 Debug 配置/编译通过。
用户退出旧工具后，修复和说明已同步树莓派并回读一致；实际 A 板启动握手及连续
3 秒通信预检通过，未发送舵机角度或启动运动。旧文件备份在
`/home/uniforest/.uniforest-sync/arm-zero-20260923-164522`。前臂角度标定仍由用户执行。

### 更换前臂舵机：独立调零入口与固定偏置

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 输入 0～180°、0.1° 分辨率的前臂指令；仅在输入后发送，不自动复位 | tools/arm_zero_adjust.py；复用 Robot 和 Transport | 非法输入与命令编码、语法/导入/协议检查；新增操作说明 |
| 等待输入期间监视通信，逐指令确认 ACK，机构忙或通信异常时中止 | 独立工具；既有心跳、遥测与动作状态接口 | 静态调用链核对；实机结果待确认 |
| 前臂固定偏置集中在 PWM 输出层，整数及小数接口共用 | A 板 servo.h / servo.c | Debug 配置/编译；核对复位、协议及动作表调用链 |

当前偏置为 0，用户尚未提供新默认角度。确认后设置为“新默认角度 − 90°”，
两轮 Task1/Task2、数量检查及 Grap/Build 均继续使用原逻辑角度，其他舵机不变。
补偿后输出限幅到 0～180°，需在得知偏置后核对端点是否可达。
协议未变：0x20 整数 2 B / 小数 3 B、大端角度、0x21 复位和 0x22 四舵机，
11 B 动作状态、80 B 遥测和 200 ms 失联急停保持。无新依赖，不启动比赛或实机动作。
实机入口为 `tools/arm_zero_adjust.py`；正式启用实际偏置须经 CLion 烧录，现场标定待用户操作。

验证：本地 61 文件语法、导入、现有协议格式检查及 Debug 配置/编译通过；
角度边界、非数值拒绝、整数/小数实际协议编码和文档链接检查通过。
独立工具及说明已同步树莓派，逐文件回读一致，远端既有通信接口和 `--help` 检查通过，
DAPLink 稳定串口路径指向 `/dev/ttyACM0`。部署目录记录为
`/home/uniforest/.uniforest-sync/arm-zero-20260923-163820`；未覆盖比赛程序，未烧录或执行舵机动作。

## 2026-09-21

### 文档整理与 GitHub 版本同步

| 行为变化 / 文档整理 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 汇总此前授权的长距离 1000 mm/s、Task2 坡道 800 mm/s 与显式 800 ms 加速 | control/chassis.py、Strategy/competition.py、task2.py | 两轮共用配置与调用核对，双 README 更新 |
| 更新当前版本及最近部署日期，区分仓库与树莓派版本 | 双 README | 对照 2026-09-20 部署记录，保留搜索实现差异说明 |
| 说明方块对准速度、窗口、微调降级接受条件及尚未修复的视觉冻结边界 | RaspberryPi/README.md | 对照 vision_targets.py、cube_tracker.py 与 _align_cube，不修改对准行为 |

协议未变：核对双方命令 0x10 / 8 B、四路 int16 大端转速、80 B 遥测布局、
schema v3 动作状态与 A 板 200 ms 通信失联急停。无下位机改动或新增依赖。
本地 60 文件语法、导入、协议格式检查及 Debug 配置/编译通过；这些检查不代表
实机速度、坡道效果或视觉精度通过。最新速度适用范围、实机入口和待复测状态
沿用下方 2026-09-20 记录。本次仅整理文档并上传 GitHub，不启动机器人或烧录固件。

## 2026-09-20

### Task2 两段坡道前进单独使用 800 mm/s

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 两轮 Task2 起步 2350 mm、前往 Build 2100 mm 的前进速度单独设为 800 mm/s | Strategy/task2.py 配置及调用 | 两轮继承配置核对，保持其他长距离速度 1000 mm/s |
| 两段均明确使用原 800 ms 加速，避免速度不再等于公共常量后退回 300 ms | Strategy/competition.py 的可选加速参数 | 语法/导入/协议与 Debug 编译，核对调用参数 |

2026-09-20，用户说明上述两段分别经过上下坡。路线距离不变，仅速度从 1000 调整为
800 mm/s（0.8 m/s），加速仍为 800 ms。Task0、Task1 长距离和第一轮 Build 后左移
继续使用 1000 mm/s；普通定距及顶墙速度保持。无新依赖，无需重新烧录。
协议未变：0x10 / 8 B、大端字段、80 B 遥测、状态 6 和 200 ms 失联急停保持。
入口为 Task2 两轮或完整比赛；未执行实机动作，坡道效果待用户复测。
验证与部署：本地 60 文件语法、导入、协议与 Debug 配置/编译通过；两轮配置确认
两段前进 800 mm/s、加速 800 ms，其他长距离仍 1000 mm/s。树莓派四文件同步回读一致，
远端 68 文件语法、导入及协议检查通过。旧文件备份于
`/home/uniforest/.uniforest-sync/task2_ramps-20260920-214747`。未启动机器人。

### 长距离移动速度调整为 1000 mm/s

本节保留当时调整记录；同日随后将 Task2 两段坡道单独改为 800 mm/s，最终配置见上一节。

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 长距离目标速度 750→1000 mm/s，加速时间仍为 800 ms | control/chassis.py 常量；Task0、两轮 Task1/Task2 引用 | 核对各路段配置、语法/导入/协议检查、Debug 配置与编译；双 README 更新 |

适用 Task0 前进 1200 mm、两轮 Task1 投放前进、两轮 Task2 起始前进 2350 mm 和前往
Build 的 2100 mm，以及第一轮 Build 后左移 2500 mm。手动定距 move 的默认速度也
继承该常量。普通定距 400 mm/s、300 ms 加速，常规顶墙 300 mm/s 和短压墙 150 mm/s
保持；路线距离、舱门等待及机构并行节点不变。
协议未变：底盘 0x10 / 8 B、大端字段、80 B 遥测、动作状态 6、200 ms 失联急停均未改。
仅上位机参数，无新依赖，无需烧录。实机入口为比赛 all/round1/round2 或对应任务，
1000 mm/s 现场效果待用户确认；不自动启动机器人。
验证与部署：本地 60 文件语法、导入、协议格式及 Debug 配置/编译通过；已核对所有
引用的长距离速度为 1000 mm/s、加速 800 ms。树莓派三文件同步回读一致，远端
68 文件语法、导入与协议检查通过。备份位于
`/home/uniforest/.uniforest-sync/long1000-20260920-213239`。未启动机器人或烧录固件。

## 2026-09-19

### 当前文档与 GitHub 版本整理

| 行为 / 文档变化 | 受影响模块 | 验证范围 |
| --- | --- | --- |
| 归并 300 mm/s 顶墙、Task1 舱门等待、Grap/Build 联动及底盘并行的最新实现 | 已有未提交的策略、动作客户端、底盘监督及 A 板动作表 | Python 语法/导入、协议格式、Debug 配置与编译 |
| 当前协议说明统一到 schema v3，说明状态 6 仍忙及版本不匹配报错 | 双端 README、PROJECT.md、schema/常量/格式测试 | 动作启动 6 B、状态 11 B、大端字段、80 B 遥测和急停行为核对 |
| 机械动作完整流程集中到 ACTIONS.md，README 保留入口和摘要 | 根 README、双端 README、Uniforest_A/ACTIONS.md | 对照动作表及两轮配置，保留历史记录而不作为当前参数 |

本次整理不新增动作参数，不修改协议行为；提交包含此前已经授权的源码改动。
最近部署为 2026-09-18：状态 6 配套运行文件及 Task1 舱门 300/0 ms 已同步树莓派，
远端 68 文件语法、导入及现有协议检查通过。Git 工作区的橙色边缘恢复逻辑尚与树莓派
版本不同，此前同步有意保留树莓派既有搜索实现；本次 GitHub 上传不等于树莓派全量同步。
树莓派上的下位机源码仍含旧参数，烧录以本仓库开发机 CLion 工程为准。
现场日志确认 A 板已产生 state=6；未据此宣称所有新时序或整场比赛已实测通过。
本次不启动机器人或烧录固件；后续小范围实机复测由用户进行。
验证：2026-09-19 本地 60 文件语法、导入、6 项协议格式检查、Debug 配置/编译和文档链接检查通过。

## 2026-09-18

### Task1 前三个橙色投放：开舱等待 300 ms，关舱不等待

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 投放步骤 8 开舱后等待 500→300 ms；步骤 10 关舱后等待 500→0 ms | Strategy/competition.py、control/actions.py | 核对两轮共用入口及其余调用；语法/导入/协议和 Debug 编译 |

2026-09-18，适用两轮 Task1 正式比赛投放入口。开舱 A=130°、B=50° 后等待 300 ms，
再后退 300 mm；关舱 A=63°、B=117° 后直接执行左移和转向。固定等待减少 700 ms。
新增 hatch_close 的 settle_ms 参数，默认仍为 500 ms，仅 Task1 明确传 0；
Grap/Build 的下位机动作表未改。协议未变：命令编号、载荷长度、大端字段、80 B 遥测、
state=6 并行状态及 200 ms 失联急停保持。无新依赖；无需重新烧录，现场效果待用户复测。
验证与部署：本地语法/导入/协议检查及 Debug 配置、编译通过；树莓派四个文件同步后回读
一致，远端 68 文件语法、导入和协议检查通过。旧文件备份于
`/home/uniforest/.uniforest-sync/task1-hatch-wait-20260918-174553`。未启动机器人。

### 修复实机 state=6 被旧上位机误判为故障

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 接受 ACTION_CHASSIS_READY=6 并在 Grap2 回程 5 cm / Build 最后释放节点继续底盘路线 | control/actions.py、control/chassis.py、Strategy/competition.py、task2.py、protocol/ | 同步配套上位机，校验报文解码和并行监督；不修改急停规则 |

2026-09-18，现场报错 `A-board action failed: state=6, stage=7`。SSH 读取确认树莓派
运行旧动作客户端（只接受 RUNNING/DONE），配套的新状态定义、并行路线与监督代码未同步；
下位机已能发出状态 6。本次补齐树莓派版本，保留树莓派既有橙色搜索实现和 300 mm/s
常规顶墙参数。0x50 / 6 B、0x83 / 11 B、大端载荷、80 B 遥测与 200 ms 失联急停未变；
状态语义与 schema v3 配套。无需为此问题改写或重新烧录下位机，不自动启动实机动作。
测试入口为 Task2；现场重试结果待用户确认。
部署结果：已同步树莓派 10 个上位机运行/文档文件，逐文件回读一致；旧文件备份在
`/home/uniforest/.uniforest-sync/overlap-state6-20260918-170114`。远端 68 文件语法、导入、
现有协议检查，以及 state=6 解码、状态常量和并行入口检查通过。本地 Debug 编译通过。
树莓派下位机源码及其既有参数未覆盖；未烧录、未自动启动比赛，现场动作待用户复测。

### Grap2 / Build 到达指定节点后并行执行后续底盘路线

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| Grap2 回程 V 上升到 5 cm 后允许底盘离开；Build 第 14 步释放后允许离开，机构继续执行 | A 板 actions.c/actions.h，动作状态协议 | 新状态 6 仍计为机构忙；动作、停止和超时调用链核对 |
| 后续底盘路线与机构收尾并行；全部结束后才允许下一次机械动作 | control/actions.py、Strategy/competition.py、task2.py | 原路线参数不变，短压墙完成后才离开；独立测试无提前路线 |
| 并行路线中的正常转弯结束只停底盘；异常仍整机急停 | control/chassis.py | 每个控制周期检查动作状态、遥测、取消和急停代次；无后台运动线程 |
| schema v3 增加 ACTION_CHASSIS_READY=6 | protocol/commands.py、schema.json/schema.py、tools/carried_cube_count_test.py | 扩展现有协议格式检查，校验双端状态常量和 11 B 状态解码 |

2026-09-18，适用 Task2 两轮的 Grap2 后续路线，以及有 Build 后续路线的轮次。
Grap2 的里程触发取自下位机步进脉冲：回程上升 5 cm 后，剩余上升、水平回收、
释放和复位继续；Build 最后一次释放后，上升 4 cm、水平回收 23 cm 和复位继续。
上位机按 50 ms 周期查询，收到匹配 token/id 的新鲜就绪状态后开始后续路线，
不承诺底盘与脉冲触发电气同时。保持动作锁，底盘路线结束但机构未完成时继续等待。
短压墙与 Grap 同时启动，离开前需确认压墙流程结束；找不到紫块时按原路线直接继续。
`action_test.py` 和建筑对准+Build 单项入口不新增底盘路线；Build 后结束比赛的轮次
仍等机构完成。新上位机遇到旧固件仅在 DONE 后继续，不能获得提前并行效果。

协议帧布局未变：0x50 / 6 B、0x83 / 11 B、大端字段、80 B 完整遥测不变；
协议语义有扩展，新增状态 6 表示仍忙且允许底盘后续动作，不能当作 DONE。
旧上位机不识别状态 6，需同步新上位机并通过 CLion OpenOCD + DAPLink 烧录新固件。
30 秒运动组/120 秒机构超时、200 ms 失联急停仍生效；状态丢失/陈旧、重启、取消、
底盘发送失败或路线异常立即整机急停，不自动恢复。无新依赖，未上传、未烧录、未实机验证。
验证：60 文件 Python 语法、导入、6 项协议格式检查及 Debug 配置/编译通过。
临时执行 8 项客户端/报文边界检查（就绪、旧固件、单动作、取消、陈旧、重启、路线异常、
短压墙尚未结束），以及正常转弯停车/取消后禁止发速度的检查；均通过。未增加虚拟机械
轨迹或硬件测试文件，这些结果只验证软件分支，不代表机构并行运行的现场效果。

### Build 上升到 15 cm 时提前启动第 14 步（需重新烧录）

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 第 12 步上升 20.5 cm 与第 13 步同时启动；上升到 15 cm 时启动水平伸出 23 cm | actions.c Build 表，复用 DUAL3_ASYNC | 核对现有步进中断交叉触发和两轴汇合；双端 README 同步 |
| 完整上升结束且水平伸出达到 22 cm 后下降 4 cm；两轴及舵机序列完成后释放 | 现有 Stepper_StartMoveOverlap3 与 JOIN | 核对不截断上升、不提前释放、不增加固定等待 |

2026-09-18，适用两轮比赛及 `action_test.py build`。上升最后 5.5 cm 与水平伸出重叠；
第 13 步角度与 500 ms 分段等待保持。第 14 步后继续原第 15 步，无新依赖。
协议未变：动作启动 0x50 / 6 B、动作状态 0x83 / 11 B、大端字段、80 B 完整遥测及
200 ms 失联急停保持；动作内部阶段索引随表更新。30 秒运动组/120 秒整套超时仍覆盖
并行段。未烧录、未执行实机动作，需 CLion OpenOCD + DAPLink 烧录后由用户验证。
验证：60 文件 Python 语法、导入、6 项协议格式检查及 Debug 配置/编译通过。

### Build 第 10～13 步按进度联动（需重新烧录）

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| V 上升 5 cm，在 1 cm 处启动 H 回收 23 cm；H 回收到 18 cm 时 V 下降 11.5 cm，并执行前臂 95°、翻转 102.2°、吸气 | actions.c Build 表；复用现有 Stepper_StartMoveOverlap3 | 核对脉冲进度触发、两轴结束条件；双端 README 同步 |
| 两轴结束后，V 上升 20.5 cm 与第三次 LIFT 同时启动，两者完成后才进入第 14 步 | actions.c 非阻塞并行执行与汇合 | 核对等待期间仍检查运动超时、取消清理并行状态 |

2026-09-18，适用两轮比赛及 `action_test.py build`。下降由步进中断触发，舵机/吸气
由主循环检测同一回收 18 cm 阈值后执行，存在主循环调度间隔；未作硬件同步承诺。
第三次抬臂仍为 50°→28°→14°→6°→0°，每档 90 ms，末尾 50 ms，随后翻转 97.2°。
等待指令合计仍为 2500 ms，第三次抬臂的 500 ms 与 V 上升重叠，不再串行相加。
协议未变：0x50 启动 6 B、0x83 状态 11 B、大端载荷、80 B 完整遥测、200 ms
失联急停和显式急停保持；仅动作内部阶段索引随动作表变化。30 秒运动组/120 秒
整套超时继续覆盖并行阶段，无新依赖。未烧录、未执行实机动作，现场结果待用户确认。
验证：60 文件 Python 语法、导入、6 项协议格式检查及 Debug 配置/编译通过；
核对了进度等待、并行汇合、超时和急停清理路径，未新增虚拟硬件测试。

### 所有 Grap 删除步进结束后的 100 ms 等待（需重新烧录）

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| Grap1/2/3 每组步进完成后直接执行下一条动作；Build 已无该等待 | Uniforest_A/Core/Src/actions.c 的 waiting_move 分支 | 核对仍等待两轴实际完成，双端 README 同步 |

在上一条等待精简基础上，Grap1/2 各再减少 200 ms，Grap3 再减少 300 ms。
Build 步骤 3/4/7/8/13 的等待及 Grap 独立测试结束后的 1000 ms 保留。
适用两轮比赛和 action_test.py。协议未变，步进忙检测、30 秒单段/120 秒整套超时、
动作状态及 A 板 200 ms 失联急停保持；无新依赖。未烧录、未实机验证，需通过
CLion 的 OpenOCD + DAPLink 配置烧录后生效。


### Grap / Build 删除指定等待（需重新烧录 A 板）

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| Grap1/2 删除去程和回程结束后的显式 300 ms 等待；Grap3 无该等待 | Uniforest_A/Core/Src/actions.c 动作表 | 核对距离、方向、舵机和吸盘操作顺序不变 |
| Build 仅保留步骤 3/7 的 500 ms 和步骤 4/8/13 的分段抬臂等待 | actions.c Build 表和 LIFT 宏 | 其余显式等待及 Build 每组步进结束后的 100 ms 删除；双端 README 更新 |
| 等待步进实际完成、动作状态和急停机制保持 | actions.c 执行器、上下位机协议 | 协议未变；动作启动 0x50 / 6 B、状态 0x83 / 11 B、80 B 完整遥测和 200 ms 失联急停未改 |

适用两轮比赛及 action_test.py 的 Grap1/2/3、Build 入口。Grap 的每组步进后 100 ms
保留，Grap 测试模式末尾的 1000 ms 保留；Build 固定等待合计 2500 ms，不包含电机
运动耗时。吸盘释放仍是非阻塞开阀，未改变阀定时。无上位机协议或依赖修改。
2026-09-18：未烧录、未执行实机动作，新等待时序需在 CLion 的 OpenOCD + DAPLink
配置烧录 A 板后由用户验证。此前常规顶墙 300 mm/s 的未提交修改保留。
验证：动作表除等待外的操作序列逐项一致，Build 固定等待核算为 2500 ms；
60 文件 Python 语法、导入、6 项协议格式检查与 Debug 配置/编译通过。


### 常规顶墙恢复为 300 mm/s

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 常规顶墙 250→300 mm/s | Strategy/competition.py 公共配置；Task1/Task2 全部常规顶墙调用 | README 同步，Python 语法/导入/协议和 Debug 编译检查 |

适用两轮 Task1/Task2，入口为正式比赛程序；最长仍为 4 秒。短压墙保持
150 mm/s、最长 1 秒并与抓取同时启动。保留已回退后的分段移动和转弯逻辑。
协议未变：0x10 / 8 B 大端底盘速度载荷、80 B 遥测、A 板 200 ms 失联急停均未改。
无依赖或下位机修改。60 文件语法、导入、6 项协议检查与 Debug 配置/编译通过。
未执行实机动作，300 mm/s 的当前现场效果待用户验证。

## 2026-09-16

### 常规顶墙速度改为 250 mm/s

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 常规顶墙 300→250 mm/s | Strategy/competition.py 的公共配置；Task1/Task2 全部常规顶墙调用 | README 同步；语法、导入、协议及 Debug 编译检查 |

2026-09-16，适用两轮 Task1/Task2，正式比赛入口。最长 4 秒；短压墙仍为
150 mm/s、最长 1 秒并与抓取同时启动。协议未变：命令编号、载荷、字节序、
80 B 遥测和 A 板 200 ms 失联急停未改。无新依赖或下位机修改，未执行实机动作，
250 mm/s 的现场效果待用户确认。


### 常规顶墙提速，短压墙与抓取同时启动

| 行为变化 | 受影响模块 | 检查 / 文档 |
| --- | --- | --- |
| 常规前向/左向顶墙 200→300 mm/s，最长仍 4 秒 | Strategy/competition.py、task2.py 继承配置 | 核对所有调用；README 更新 |
| 三处短压墙与 Grap3/Grap2/Grap1 同时启动，仍 150 mm/s、最长 1 秒 | Strategy/competition.py、task2.py、control/actions.py | 核对启动、两者完成、取消和通信故障路径 |
| 抓取等待期间以非阻塞回调更新压墙；短压墙结束只停底盘 | control/actions.py、现有底盘/动作接口 | 协议未变；核对下位机动作忙状态允许底盘命令及急停取消机制 |

适用两轮 Task1/Task2，包括橙色补抓；入口为正式比赛程序。保留短压墙堵转确认、
超时默认成功和橙色抓取短压墙结束时的航向重校准。抓取动作与短压墙分别完成后
才继续；动作拒绝、取消、遥测陈旧或通信故障终止组合动作，无后台线程继续发速度。
串口上的动作启动与底盘速度命令连续发送，不承诺硬件同一时刻触发。
协议未变：底盘 0x10 / 8 B、动作启动 0x50 / 6 B（大端）、80 B 完整遥测及
A 板 200 ms 失联急停保持；无新增依赖或下位机修改。2026-09-16 新参数及并行动作
待用户实机确认，未执行硬件动作。本地 60 文件语法、导入、6 项协议检查及 Debug
配置/编译通过；另对启动顺序、两者完成、提前接触、取消、拒绝、遥测陈旧、发送失败
和严格超时做了 8 项离线故障路径检查，通过仅表示代码流程符合预期，不代表现场效果。

### 文档与 GitHub 版本整理

更新项目总览、上位机和下位机说明：集中呈现当前数量检查/补抓流程、300/500/200 ms
时序、抓取后新遥测静止确认及步进 83 μs 参数。删除当前使用说明中的过期 C 虚拟
测试命令，保留以下历史记录；明确代码/实拍回放/现场验证的区别及树莓派定向部署差异。

本次整理验证：60 个 Python 文件语法、依赖导入、6 项协议格式检查、Debug 配置/编译
通过；文档相对链接与提交内容检查通过。未执行实机动作。

| 行为变化 | 受影响模块 | 验证 / 文档 |
| --- | --- | --- |
| 携带数量检查与补抓 | Strategy、robot、control、vision、独立配置/入口 | 22 组实拍回放；数量检查说明记录最新时序，比赛衔接待现场确认 |
| SEQ 匹配 ACK、检查期间通信保护 | protocol/transport.py、robot.py | 协议格式与接收匹配核对；协议未变 |
| 既有步进默认目标 100→83 μs | control/stepper.py、transport.py、A 板 stepper.h/actions.c | Python 默认参数与固件编译；烧录和物理速度待现场确认 |
| 已授权的测试清理和桌面入口 | tests、tools/check.py、desktop_task.sh、A 板 tests | 保留语法、导入、6 项协议格式检查及 Debug 编译，历史失败记录不删改 |

## 2026-09-15

### 数量检查等待更新为 300/500/200 ms

按用户最新要求：翻转 37.2°后等待 300 ms、前臂 120°后等待 500 ms；丢弃 3 帧、
采集 8 帧，按下方 30% 橙色宽度识别，仓内紫色计 1。前臂复位 90°后等待 200 ms，
再将翻转复位至 97.2°，清除检查视角旧结果并返回数量。比赛两轮 Task1/Task2 与
独立入口同步，固定等待合计 1 秒。协议未变，无识别阈值、依赖或下位机修改；
保留抓取后等待新遥测确认静止的修正。新时序待用户现场验证。

### 修正 Grap3 后数量检查的瞬时静止判定

用户实机第三次 Grap3 后在检查前报 requires stationary robot。原检查仅看一帧，
动作状态与完整遥测独立到达时可能读取旧状态；原日志没有忙标志和轮速，具体触发项
无法追溯。改为确认新 ACTION_DONE，发送底盘零速，最多等待 1 秒内连续 3 帧新遥测
确认步进空闲及四轮绝对速度不超过 10 rpm，再执行检查。只计动作完成后新采样，
失败输出忙标志、轮速、帧数；不通过取消/通信故障自动续跑。Task1/Task2 共用修正。
协议未变，使用已有底盘零速命令；舵机等待 200/300 ms 和连续复位保持原实现。
无新增依赖或下位机修改，新衔接待用户实机复测。

### 数量检查时序缩短

按用户要求，翻转 37.2°后等待 200 ms、前臂 120°后等待 300 ms；丢弃 3 帧、
采集 8 帧及底部 30% 宽度识别保持原实现。复位连续发送前臂 90°与翻转 97.2°，
保留各命令 ACK 确认，移除两者之间的 300 ms 等待；正常后清除检查姿态旧帧。
固定等待从 1000 ms 减至 500 ms，比赛两轮 Task1/Task2 与独立入口同步。
协议未变，无新增依赖、下位机或识别阈值修改。新时序待现场验证；原标定照片
的 calibration.pose 历史参数保留，不冒充新时序采样结果。

### Task1/Task2 接入携带数量检查和补抓

两轮 Task1/Task2 完成原定橙色抓取批次后执行检查：3/null 继续，0/1/2 补抓
3/2/1 块并复查；任一抓取批次因搜索距离耗尽结束则跳过检查。搜索距离、橙色
恢复状态和编码器原点共用，后续横移补偿包括补抓。保留 Grap3/Grap1 抓取流程。
复用 Robot 串口心跳和 cube 原始帧，按已实拍检查姿态执行；正常后复位并清除
检查视角旧帧。通信/ACK/采集/取消异常中止，不纳入 null 默认成功分支。
协议未变；新增主机侧 SEQ 匹配 ACK 等待，编号、载荷、字节序、80 B 遥测及
A 板 200 ms 失联急停均保持原实现，无新依赖、无下位机改动。详细行为清单、
动作时序和验证边界见携带检查说明。组合比赛流程待用户实机验证。

### 携带数量改用底部 30% 橙色宽度

按用户要求移除原上边缘/空间外观特征。仅截取 x=0..639、y=336..479，橙色
HSV [0,80,40]～[50,255,255]，取最大区域逐行宽度中位数，再取 8 帧中位数。
feature_version=4；紫色计 1 规则同步限制于下方 30%，上方画面不参与判断。
用原已确认各数量样本及 1 块偏移样本重提一维归一化宽度原型：0、245.5/234、
406、640 px；距离/帧间差异上限 0.12，最小类别距离差 0.02，保持偏移容差。
22 组 176 张真实照片回放均有输出，已确认标签相符；包含训练样本，非独立准确率。
具体参数、适用条件、行为清单见 tools/carried_cube_count_test.md。协议未变，
双方命令编号、载荷、字节序、遥测和急停保持原实现；无新增依赖、不接入比赛。

### 携带数量正常偏移容差

用户确认 9 月 14 日 23:47:49 仓内为 1 块，并要求允许正常摆放偏移。将该组
8 张真实图像中位数加入 1 块补充参考，保留原标准位置；接受距离/帧间差异上限
由 0.10 调整为 0.12，最小类别差距由 0.10 调整为 0.06。这些容差为本次暂定参数，
没有毫米级偏移保证。22 组 176 张实拍回放均返回数量，该组返回 1；包含训练样本，
不视为独立准确率，需用户继续新摆放验证。原始照片和结果保留，另附人工确认记录。
协议未变，执行机构参数与比赛流程保持原实现。行为清单和参数见独立入口说明。

## 2026-09-14

### 携带数量第三版：分离仓内前景与中央仓外方块

用户新拍图像显示，第二版的中央 ROI 仍包含仓外堆叠方块。第三版只保留连接
图像底部的宽色块，排除上方分离方块及亮度；下部仓内亮度保留用于空仓判别。
重提原标定特征，feature_version=3，距离/类别差距阈值仍为 0.10；支持同类多
摆放参考，但尚未加入未确认数量的新图。22 组 176 张实拍回放，21 组返回数量；
23:47:49 因顶边位置超出原 1 块参考覆盖范围仍返回 null，待用户确认实际数量后
补充参考。新组 23:48:01、23:48:19、23:49:18 分别返回 1/1/2，需人工核对。
本地语法、导入、6 项协议检查及 Debug 配置/编译通过。协议未变，命令编号、
载荷、字节序、遥测与急停行为均保持原实现；执行机构、依赖、比赛流程不变。
具体行为清单、像素区域、连通规则及适用限制见独立入口说明；助手未操作实机。

### 携带数量第二版：仓内紫色与仓外干扰修正

根据用户多轮真实图像定位四组 null：一组仓内紫色不匹配橙色模板，另三组仓外
橙色进入原 ROI，匹配距离超过阈值。数量区域缩为 640×480 下 x=250..399、
y=120..479，使用同一批已确认的四类图像重新提取原型，拒绝距离/间距保持 0.10。
仓内下部独立检测紫色，8 帧至少 6 帧满足面积和尺寸要求后按用户规则计 1。
新增 reason/method 便于区分未知原因，具体参数和行为清单见独立入口说明。
回放 14 组、112 张实拍图像，原四组 null 分别变为 1/2/2/2，仓外紫色空仓仍为 0。
后三组没有人工标签，新版本仍需用户自行新拍确认；未增加模拟视觉测试。
仅独立入口使用，协议未变，舵机角度、等待、曝光、依赖与 A 板控制均未改动。

### 携带数量第一版实拍参数（待独立验证）

用户自行完成 0/1/2/3 块橙色方块拍摄，各使用一组 8 帧。明确排除 22:47:13
误标为 3 块的实际 2 块图像；3 块采用 22:47:48 第二次拍摄。ROI 设为归一化
[0.2,0.2,0.6,0.8]，四类特征为各组中位数，接受距离/最小类别差距暂为 0.10。
各类训练图像回放相符，误拍组额外回放识别为 2；这些不替代独立摆放验证。
适用范围仅独立携带检查，不接入比赛；紫色/混合颜色未验证。协议未变，
相机曝光、增益和舵机动作参数保持原值；助手未远程执行本轮实机动作。

### 携带数量重新标定准备与启动握手修正

隔离原有携带数量样本，清除本地合成样例图；数量配置没有有效原型，保持
ROI/阈值未标定，其他相机标定不变。首次空仓尝试未发送舵机命令即中止。
实际串口诊断显示旧遥测（约155秒）在当前PONG（约38秒）前被读出，导致
错误重启判定；独立入口改为PONG握手后建立遥测基准，运行期间保护保留。
协议未变。后续实机动作按用户要求由用户自行执行，助手只读图像并做标定。

### 按用户要求精简测试程序

删除 20 个 Python 测试文件及 4 个 A 板虚拟硬件测试/轨迹文件，合计约
4502 行，包括模拟机器人/时钟、合成视觉、旧路线参数断言，以及部分低收益
辅助测试。刚新增的携带数量模拟测试也一并移除，其实机入口和标定采样功能保留。
保留 `test_protocol_schema.py`、`test_stepper_dual3.py` 的 6 项协议格式检查
和 `import_smoke.py`；移除协议检查中的 Mock 及源码字符串断言。

`tools/check.py` 改为语法、导入、指定协议检查，成功只输出简短结果，不自动
发现全目录测试；移除 `--module` 与 `--native-actions`。保留 `--firmware`
编译选项及所有动作、相机、图像回放和实机调参入口。不改变比赛程序或协议。
后续调参使用真实图像、串口反馈与现场结果，不以模拟通过替代实机验证。
下文原全量/专项测试数字均为清理前历史记录；删除测试不表示修复了旧失败。

### 携带方块数量检查：独立测试与标定采样

新增 `tools/carried_cube_count_test.py`、独立配置、说明以及
`vision/carried_cube_count.py`，不接入比赛入口。执行翻转 37.2°、等待 300 ms、
前臂 120°、等待 400 ms、采集识别、前臂 90°、等待 300 ms、翻转 97.2°。
存储区尺寸按用户提供的 100×100×300 mm 记录；所有新姿态/等待参数尚待现场
验证，适用轮次仅为独立测试。记录原图、ROI 特征、相机设置和人工数量标签。
默认尚未标定，返回未知，不把俯视轮廓个数当成堆叠数量。

协议未变，命令、字节序、载荷、80 B 遥测和 200 ms 失联急停均已核对；
不修改 A 板。测试入口按序号匹配舵机 ACK，并对失联、陈旧反馈及 Ctrl+C 中止
锁存，不自动续跑。具体行为变化清单和现场待确认项见
[独立测试说明](tools/carried_cube_count_test.md)。保留原有步进调速等未提交改动。

本地新增 12 项测试及 6 项协议测试通过；语法、依赖导入、离线图像保存与
`--dry-run` 通过；Debug 配置/编译通过，未烧录。全量测试仍有既有
`test_orange_clusters` 的 11 个断言失败（含子测试），不据此宣称全量通过。
未执行实机动作，数量标定待后续分数量采样完成。

树莓派已上传并核对新增文件；专项 18 项通过。远端全量 173 项有 21 个失败、
4 个错误，排除新增测试后的 161 项基线仍相同；已记录，未改动远端比赛模块。

## 2026-09-13

### 步进电机巡航速度提升

Grap1、Grap2、Grap3 和 Build 的步进电机巡航目标由约 4167 步/秒提升至约
5020 步/秒：目标半周期延时由 100 μs 改为 83 μs，起步延时仍为 1000 μs，
加速/减速仍为 400 步，S 曲线保持不变。同步修改上位机默认参数、下位机四类
动作调用和 README。协议格式未变；需重新烧录 A 板，尚未实机验证。
本地 19 项动作/协议测试、Python 检查和 A 板 Debug 构建通过。

## 2026-09-12

### 树莓派桌面任务入口

新增 tools/desktop_task.sh，在树莓派 Desktop 创建全任务流程、round1、round2
三个 .desktop 入口。固定工作目录和虚拟环境，显示实时日志，结束保留终端，
支持 Ctrl+C；入口共用 flock 防止重复启动。README 同步操作说明。
只包装现有 main.py 参数，不更改任务编排、视觉、电控参数；协议未变。
安装验证不启动真实任务。
已安装到 `/home/uniforest/Desktop`，设置可执行权限及桌面信任标记。
`bash -n`、`desktop-file-validate` 通过；隔离临时目录用假解释器验证三种参数、
工作目录、退出码和重复启动锁通过。未运行真实机器人任务。

## 2026-09-13

### 步进电机巡航速度

Grap1、Grap2、Grap3 和 Build 使用的步进电机巡航目标由约 4167 步/秒提升为约
5020 步/秒（目标半周期延时由 100 μs 改为 83 μs，受 6/5 安全倍率和 10 μs
TIM7 tick 量化影响）。加速/减速仍为 400 步，起步延时仍为 1000 μs。上位机
默认参数、下位机动作参数和文档已同步；协议格式未变。需用户重新烧录 A 板后生效，
本次仅完成编译验证，未执行实机动作。

## 2026-09-10

### 橙色回找速度与范围扩大

两轮 Task1 / Task2 共用的向左回找速度从 120 改为 200 mm/s，距离上限从 300
改为 1000 mm；超时从 3 延长至 6 s，留足 5 s 行程及停车确认时间。阶段起点、
候选确认、故障退出和 1800 mm 向右搜索预算规则不变。
影响：Strategy/competition.py 参数、test_orange_search 速度/长距离预算验证及 README。
协议未变：命令编号、载荷长度、字节序、遥测布局和 A 板 200 ms 失联急停未改动；
无需重新烧录。定向入口：`python tools/check.py --module tests.test_orange_search`。
现场入口仍为 `python main.py --task task1-r1` / `task1-r2` / `task2-r1` / `task2-r2`；
参数按用户要求修改，尚未实机确认。
本地语法、导入及 12 项回找专项测试通过；连续截断仿真确认可回找约 1000 mm，
不再受原 3 s 超时限制。本次为参数修改，未重复全量测试或下位机构建。
树莓派已同步本次 4 个文件并核对哈希；远端读取确认两轮 Task1/Task2 均为
200 mm/s、1000 mm、6 s。未执行实机动作。

### 橙色左缘出画时有限向左回找

| 行为变化 | 受影响模块 | 测试 / 文档 |
| --- | --- | --- |
| 有效 ROI 内的大橙色簇触及左边界时，即使没有几何候选也发布逐帧提示 | vision/orange_cluster.py、cube_detector.py | test_orange_search 合成图：满屏排、上方 ROI、噪点、右侧裁剪、分辨率和配置切换 |
| 无有效候选且连续 3 个新帧提示截断，停车后以 120 mm/s 左移回找 | Strategy/orange_search.py、competition.py、task2.py | 仿真回找成功、候选确认、同排关联、搜索极限和失败退出 |
| 单次左移最多 300 mm / 3 s，不越过橙色阶段起点；连续截断只尝试一次 | 同上 | 起点限制、编码器不动、时间限制及防振荡 |
| 1800 mm 只累计正常向右搜索指令时间，停车与左移独立；净位移补偿继续使用编码器 | 同上 | test_search_exhaustion、Task1/Task2 路线测试 |
| 新搜索遇遥测/图像失效、发送失败或期间收到本地急停立即退出，不当作搜索耗尽继续 | control/chassis.py 返回发送结果、protocol/transport.py 本地急停计数 | test_orange_search、test_protocol_schema |

适用：两轮 Task1 / Task2 橙色阶段；紫色和 Build 视觉不启用回找。回找前、恢复
向右前停车 100 ms；距阶段起点保留 10 mm 加一个控制周期的左移指令距离。
一次回找后须连续 3 个新帧无左侧截断，且净右移超过上次触发位置 100 mm 才重新
允许回找。同排关联使用原截断区域与候选四边形的纵向重叠，不能保证区分所有重叠物。

协议未变：双方命令编号、载荷长度、字节序、80 字节遥测布局、A 板 200 ms 通信
失联急停保持原样；新增急停计数仅在上位机内存中使用，无需重新烧录，无新增依赖。
测试入口：`python tools/check.py --module tests.test_orange_search --module tests.test_search_exhaustion --module tests.test_protocol_schema`。
现场入口：`python main.py --task task1-r1` / `task1-r2` / `task2-r1` / `task2-r2`。
当前参数为初始建议值，尚未实机确认；编码器边界不能消除地面打滑或停车惯性。
本地验证：上述新测试及搜索耗尽、Task1、Task2、协议、视觉配置共 79 项通过，
Python 语法和导入检查通过，A 板 Debug 配置与构建通过。旧搜索测试补入持续更新
的空画面和遥测；首帧候选即停车，因此确认期间不再消耗向右里程。未执行实机动作。
树莓派 `/home/uniforest/Uniforest/RaspberryPi` 已同步本次 14 个文件并核对 SHA-256；
远端语法、导入及回找/搜索耗尽/协议 22 项检查通过（OpenCV 4.10.0）。

## 2026-09-07

### 第一轮 Build 后退距离

`Task2Config.post_build_reverse_mm` 从 200 改为 100 mm，速度仍为 400 mm/s、加速
300 ms；第二轮仍在 Build 后结束。对应更新路线测试和 README。协议未变，无需
重新烧录。按用户要求，简单参数修改仅做针对性验证，本次使用
`tests.test_task2.Task2Tests.test_partial_task_order_and_parameters`。
现场入口：`python main.py --task task2-r1`；新增距离尚未实机确认。

### 整轮测试包含 Task0

`round1` 改为 Task0 → Task1-R1 → Task2-R1，`round2` 改为 Task0 → Task1-R2 → Task2-R2。
模块：`Strategy/runner.py` 编排表和 `main.py` 帮助；测试/文档：`test_task_runner`
覆盖两轮顺序、完整流程只运行一次 Task0、单任务跳过 Task0、Task0 失败阻止后续
任务，双方 README 同步入口说明。Task0 参数仍为前进 1200 mm、750 mm/s、800 ms
加速。协议未变：命令编号、载荷长度、字节序、80 字节遥测及急停路径未修改，
A 板 200 ms 通信失联停止不变；无需重新烧录，无新增依赖。
适用入口：`python main.py --task round1`、`python main.py --task round2`；
现场结果：按用户要求调整，尚未实机验证整轮起步。
本地验证：语法、导入、任务编排/Task0/协议 15 项及 A 板 Debug 构建通过。

### 紫色搜索距离分轮次调整

第一轮 Task2 紫色累计向左搜索上限改为 750 mm，第二轮改为 650 mm；由
`Task2Config.purple_search_max_distance_mm` 和 `Task2Round2Config` 分别设置。
对应更新 `test_task2` 的配置、完整路线和两轮搜索调用断言，以及 README 轮次表。
搜索仍为 300 mm/s，紫色上方 40% ROI、搜索耗尽跳过 Grap2 和后续路线补偿不变。
协议未变：双方命令编号、载荷长度、字节序、80 字节遥测和急停路径均未修改，
A 板 200 ms 失联保护不变，无新增依赖，无需重新烧录。
现场入口：`python main.py --task task2-r1` 或 `--task task2-r2`；
现场结果：按用户要求修改，尚未实机验证新增搜索距离。
本地验证：语法、导入、Task2/紫色 ROI/协议相关 36 项及 A 板 Debug 构建通过。

### 紫色 ROI 与测试流程整理

| 行为变化 | 受影响模块 | 测试/文档 |
| --- | --- | --- |
| 两轮紫色搜索/对准屏蔽上方 40%，只检测下方 60% 的紫色目标 | vision/cube_detector.py 新增 task2_purple；Strategy/task2.py 自动切换并在 finally 恢复 default | 两种分辨率的上下目标隔离、下方目标坐标不变、缓存清除和成功/耗尽/异常恢复 |
| 紫色搜索耗尽与橙色共用 SearchRangeExhausted，移除按异常文本判断的分支 | Strategy/task2.py | 紫色跳过抓取及普通异常传播 |
| 语法、导入、全量单测统一到 tools/check.py，可选固件编译和 C 动作测试 | tools/check.py、tests/import_smoke.py | 完整运行验证退出码；导入失败不再返回成功 |
| 清理无调用的旧视觉示例及不参与构建的主程序副本 | vision/comp1.py、Uniforest_A/Core/Src/main.c.bak | 当前调用链及 CMake 构建核对；历史版本仍在 Git 中 |

ROI 在 640×480 时排除第 0–191 行，坐标仍以原图为准。Task2 橙色 50% ROI、
1800 mm 橙色搜索上限、600 mm 紫色搜索上限、颜色阈值、曝光与机械动作保持当前
配置。协议未变：核对 schema v2、双方命令编号、载荷长度、字节序、80 字节
完整遥测与急停路径，200 ms 失联停止不变；无需重新烧录，无新增依赖。

测试整理中修正了与当前代码不符的 Task1 标签参数/横移路线断言、平滑后复制对象
的身份断言，以及紫色搜索只给一帧却要求完成多帧确认的测试输入；未调整运行参数
或删除测试来消除失败。本地完整入口发现 138 项，剩余 11 个橙色几何断言失败
（含子测试）、0 个错误；语法、导入和 A 板 Debug 构建通过。

现场入口：`python vision/cube_detector.py --camera cube --no-gui --profile task2_purple`
只读观察，之后按 README 的预检/单动作/任务顺序验证两轮 Task2。
现场结果：ROI 已通过合成图像测试，尚未完成机器人运动中的现场确认。
清理本轮重复临时验证脚本、同步清单、日志和缓存；标定图片、已返回的相机画面及
历史备份保留。无硬件检查今后统一使用 README 中的 `python tools/check.py`。

树莓派同步校验 16 个文件；21 个历史副本移至
`/home/uniforest/.uniforest-sync/20260907-183823/retired/`，包含旧顶层模块/测试和
6 个仅用于旧 orange_new 引擎的测试，不再混入当前测试发现。远端复跑 138 项，
同样为 11 个橙色几何失败、0 错误；C 动作 2 项通过。紫色 ROI/Task2 专项 31 项
通过。对 18:23:23 的 cube 原图回放，启用前后均识别到一个紫色目标，
X=+7.8 mm、Y=-135.8 mm、Z=273.6 mm，确认该帧下方目标坐标未因 ROI 改变。

### 橙色搜索上限再次修订

按用户要求，两轮 Task1/Task2 橙色累计搜索上限从 1900 改为 1800 mm。
受影响模块为 `Strategy/competition.py` 公共配置；对应更新
`test_search_exhaustion`、`test_competition_task1` 和 README。
搜索耗尽后仍继续后续路线，紫色 600 mm 与直线超时参数保持当前配置。
协议未变：双方命令编号、载荷长度、字节序、80 字节遥测和急停路径均未修改，
无需重新烧录。测试入口为两轮 `main.py --task task1-r1/task1-r2/task2-r1/task2-r2`
（实际选择一个任务名）；现场结果：尚未实机验证搜索距离。

### 橙色搜索及直线超时后续修订

| 行为变化 | 受影响模块 | 测试/文档 |
| --- | --- | --- |
| 两轮 Task1/Task2 橙色累计搜索上限由 1500 改为 1900 mm | Strategy/competition.py 的公共配置，Task2/第二轮继承 | test_search_exhaustion；README 两轮路线 |
| 搜索耗尽时不因橙色数量不足终止任务，按已抓取数量（含零）继续卸载/Build | competition.py、task2.py；专用 SearchRangeExhausted 异常 | 两轮不足/零块、紫色有无、净位移补偿和普通异常仍急停 |
| 直线最低总超时从 5000 改为 2000 ms，估算行程后的额外余量从 1000 改为 2000 ms | control/chassis.py | test_chassis_position 虚拟时钟卡住/停车测试 |

实际总超时为 `max(2000, 行程/目标速度 + 加速 + 保持 + 2000)`，各项换算为毫秒。
短距离不再被原来的 5 秒下限拖长；长距离仍保留完成行程所需的时间。例如前进
100 mm、400 mm/s、300 ms 加速、hold=0，总超时约 2.55 秒。旋转参数未调整。
搜索速度仍为 300 mm/s，紫色上限仍为 600 mm；橙色预算按搜索时间累计，
不将视觉对准位移计入预算，后续净横移补偿继续使用编码器实测结果。

协议未变：核对双方命令编号、载荷长度、字节序、80 字节完整遥测、动作状态和
急停路径；schema v2 与 A 板 200 ms 失联保护不变。本次无需额外烧录或新增依赖。
适用轮次：两轮 Task1/Task2，直线超时适用于所有上位机位置环平移。
现场入口：`main.py --task task1-r1`、`task1-r2`、`task2-r1`、`task2-r2`；
直线单项入口 `tools/chassis_distance_test.py`。现场结果：尚未实机确认。

本次本地验证：搜索/位置环/协议 20 项通过；全量 135 项仍为原有 15 个失败
（含子测试）和 1 个错误，失败列表未变。语法、import_smoke、A 板 Debug 配置/
构建通过，未执行实机动作。无硬件回归入口：

```bash
python -m unittest tests.test_search_exhaustion tests.test_chassis_position tests.test_protocol_schema -v
```

### 行为与参数

| 行为变化 | 受影响模块 | 测试/文档 |
| --- | --- | --- |
| Grap1/2/3、Build 从 Python 迁移到 A 板非阻塞动作表，保留原有机械输出和等待 | control/actions.py、robot.py；A 板 actions.c/.h、main.c、CMakeLists.txt | test_actions_build、test_actions_grap3；C 原生时序及安全测试；双方 README 和 PROJECT |
| 新增带请求编号的动作启动、查询与状态接口；运行时忙碌保护，取消后不续跑 | protocol/schema.json、commands.py、transport.py；A 板 protocol.h/.c | test_protocol_schema；动作客户端故障与状态测试 |
| 第二轮 Task2 橙色净右移目标由 500 mm 改为继承第一轮的 700 mm，仍扣除编码器实测净右移量 | Strategy/task2.py | test_task2 的第二轮配置与正反补偿断言 |
| 两轮 Task1/Task2 普通定距平移统一为 400 mm/s、300 ms 加速 | control/chassis.py；Strategy/competition.py、task2.py | 两轮路线、四方向加速参数传递及协议测试 |

动作接口采用 schema v2：启动 `0x50` 为 6 字节 `>IBB`，查询 `0x51` 无载荷，
状态 `0x83` 为 11 字节 `>IBBBI`。命令数据按大端编码，帧 CRC 小端；旧命令和
80 字节完整遥测布局不变。A 板 200 ms 失联阈值不变，失联处理扩展为停止双步进、
吸盘并取消动作。客户端每 50 ms 查询状态，接口/启动确认上限 1 秒，状态陈旧
500 ms 报错急停。单段步进上限 30 秒，整套动作上限 120 秒。

机械动作适用于两轮和独立测试。Grap3 当前下降/返回段为 9 cm，步进完成稳定等待
100 ms，Grap 测试模式末尾等待 1000 ms。完整机械参数以 A 板动作表为准。
测试入口：`python action_test.py grap1`、`grap2`、`grap3`、`build`（每次选择一个）。
现场结果：未执行烧录或机械动作，需用户通过 CLion 更新固件后确认。

普通定距参数覆盖短距前后移动、标签后横移、橙色净位移补偿和紫色抓取后补偿前进。
750 mm/s 长距离动作仍用 800 ms 加速；搜索、顶墙、视觉及键盘参数未调整。
手动 `move` 默认仍为 750 mm/s、800 ms。700 mm 是橙色阶段的净位移目标，
不是抓取结束后固定再走 700 mm。

上述路线/速度调整本身协议未变：已核对双方命令编号、载荷长度、字节序、遥测
和急停路径。它们不另需固件修改，但同批机械动作迁移必须配套新固件。
适用入口：`python main.py --task task1-r1`、`task1-r2`、`task2-r1`、`task2-r2`。
现场结果：按用户要求修改，尚未实机确认。相关上位机代码已同步树莓派并核对文件哈希；
同步不表示已烧录或完成实机测试。

### 验证状态

本地环境为 Python 3.12.14、NumPy 2.3.5、OpenCV 5.0.0。

| 检查 | 结果 |
| --- | --- |
| Python 语法与 tests/import_smoke.py | 通过 |
| 动作客户端及协议相关 17 项 | 通过，使用下方列出的三个测试模块复跑 |
| 普通平移/路线及协议相关 11 项 | 通过，与上一组有重叠，不可直接相加 |
| 本地全量 Python 130 项 | 15 个失败（含子测试）、1 个错误，与本轮参数修改前列表相同 |
| A 板 cmake --preset Debug 与 cmake --build build/Debug | 通过，仅编译，未烧录 |
| 树莓派原生 C 动作测试 2 项 | 通过，含 7 组输出/等待时序及逐阶段取消、忙碌、超时、重复请求、时钟回绕 |

当前 Python 失败范围：Task1 路线顺序、长搜索停止和参数断言；橙色簇旋转、单块/
移动相机、紧贴三块及间隙子测试；tracker 对象身份断言。错误为 Task2 的
`test_purple_search_commands_left_lateral_motion`。这些问题未在本次文档整理中修复，
也未通过恢复已取消的等待或修改实机流程来使测试通过。

树莓派全量发现 144 项，存在相同 15 个失败和 5 个错误；额外 4 个导入错误来自
远端历史测试文件 `test_engine_dispatch`、`test_orange_new_adapter`、
`test_orange_new_merge`、`test_probe_offline_stats`，不属于当前本地检出。
远端执行发现测试时使用 `-s tests -t .`，避免顶层同名历史模块干扰。

可复现的无硬件入口，在 RaspberryPi 目录执行：

```bash
python -m compileall -q control protocol Strategy robot.py main.py
python tests/import_smoke.py
python -m unittest tests.test_actions_build tests.test_actions_grap3 tests.test_protocol_schema -v
python -m unittest discover -s tests -t . -v
python ../Uniforest_A/tests/run_actions_host.py
```

最后一项需原生 `cc`，详情见[下位机说明](../Uniforest_A/README.md)。本日另整理
根目录导航、上位机操作说明及下位机协议文档，移除过期参数和互相矛盾的流程描述。

## 2026-09-06

| 行为变化 | 受影响模块 | 测试/文档 |
| --- | --- | --- |
| 第一轮 Task2 Build 后改为后退 200 mm、顺时针转 180°、左移 2500 mm、左顶墙；第二轮不执行 | Strategy/task2.py | test_task2 路线顺序、轮次隔离和故障急停 |
| 第二轮 Task1 补偿基准由 2500 mm 改为继承第一轮的 2800 mm | Strategy/task1.py | test_competition_task1 第二轮补偿路线 |
| 根据用户反馈，将 750 mm/s 左移加速从 200 ms 改为 800 ms | Strategy/competition.py | 高速前进/左右移动加速参数传递 |
| 第二轮恢复左顶墙，两轮共用左顶墙、前顶墙、航向校准流程 | Strategy/task2.py | 两轮顶墙方向、顺序、速度和超时 |
| 位置到位窗口从 400 counts/20 RPM 改为 1000 counts/50 RPM，连续确认仍为 50 ms | control/chassis.py | test_chassis_position 到位、轮速及保持测试 |

Build 后左移速度为 750 mm/s，左顶墙 200 mm/s、最多 4 秒。该日低速横移的
200 ms 加速已被 09-07 的 300 ms 覆盖。协议未变：核对双方命令、载荷、字节序、
80 字节遥测和急停路径，无新增依赖；A 板仍执行 200 ms 通信失联保护。

适用入口：两轮 Task2、第二轮 Task1，以及 `tools/chassis_distance_test.py`。
现场结果：用户报告旧的左移加速过快；800 ms 加速、第二轮顶墙及放宽的到位窗口
尚待实机确认。相关 17 项测试、语法、导入、A 板 Debug 配置/构建通过。
当日全量从 122 项增至 124 项，均为原有 15 个失败（含子测试）、1 个错误。

## 2026-09-05

橙色检测切换为顶面簇左首方块定位，保留毫米单位和 X 右/Y 上/Z 前坐标接口、
Task2 ROI 与策略目标跟踪。当前最终实现采用 `orange_fixed_geometry.py` 内嵌的
Task1/Task2 独立固定投影；JSON 是标定记录，不是运行时加载来源。
此前“逐帧重新估计平面”的说明已过期。两任务橙色 X 偏置当前分别为 +5.0 mm，
不影响紫色和建筑。无新增依赖，协议未变。

模块：orange_cluster、orange_config、orange_fixed_geometry、cube_detector；测试：
test_orange_clusters。入口：`tools/cube_profile_probe.py IMAGE --profile default`
或 `--profile task2_orange`，以及 `vision/cube_detector.py --camera cube --no-gui`。
现场结果：尚未在比赛机器人上验证固定几何与各种间隙/遮挡情况。

当日早期检测版本记录为新增 7 项识别测试通过，全量 121 项有 5 失败、1 错误；
这是历史结果，后续固定几何及当前依赖环境应以 09-07 验证状态为准。

## 2026-09-04

cube Linux 相机设为手动曝光 312、增益 32、自动白平衡。建筑对准切换到包含
亮顶面的 building 色带，目标 X=0/Z=75 mm，横向优先并使用近距离减速带；
Purple 色带记录为 H105-155。模块：camera_settings、检测 profile、Task2Config；
入口：`tools/building_vision_probe.py`、`building_build_test.py`。
适用两轮 Task2，协议未变；现场记录不足以保证不同光照下的效果，改变曝光/安装
后须复测。当前建筑比例以源码 `132.8 * 82.4` 为准。

## 2026-09-03 及更早

- 09-03：Tag6 附近横移统一为第一轮 100 mm、第二轮 400 mm；Tag 对准采用距离、
  横向和航向闭环；新增建筑对准加 Build 独立入口。协议未变。
- 08-25：视觉目标集中至 Strategy/vision_targets.py；三种方块 X 目标均为 0，
  橙色粗调 [-20,5]/微调 [-3,3] mm，紫色粗调 [-5,5]/微调 [-3,3] mm。协议未变。
- 08-23：增加目标跳变/歧义保护、橙色微调和定距超时后编码器进度至少 90% 的策略
  容错，取消部分额外等待；不豁免遥测丢失、取消或 A 板 200 ms 失联急停。协议未变。

更早的记录不替代当前源码或现场确认。完整提交历史见 Git，备份快照默认只读。
