# A-Board DAPLink 配置 - 强制 HID 后端
source [find interface/cmsis-dap.cfg]

# 强制使用 HID 后端（DAPLink 传统模式）
cmsis-dap backend hid

transport select swd

# 目标芯片
source [find target/stm32f4x.cfg]

# 复位配置
reset_config sysresetreq

init
reset halt