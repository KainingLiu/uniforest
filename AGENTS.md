# Uniforest - RoboGame 2026 机器人开发

## 项目概述

RoboGame 2026 竞技组 Uniforest 队的电控、算法与视觉开发工作区。

涉及比赛规则、机器人设计要求或技术方案时，应先阅读规则手册 `RoboGame2026 竞技组规则手册2_1.pdf` 和队伍计划书 `RoboGame 2026 Uniforest队计划书.pdf`。涉及 RoboMaster A 板硬件时，按需查阅 `开发板说明.pdf` 和 `RoboMaster 开发板A型 原理图.pdf`。

## 功能分区

- `RaspberryPi/`：上位机程序。运行于 Raspberry Pi 5 8GB，负责算法、视觉、策略以及与下位机通信。
- `Uniforest_A/`：下位机程序。运行于 DJI RoboMaster A 板（STM32F427），负责底层电控、传感器采集和执行机构控制。
- `备份/`：按日期或版本保存每天工作结束时复制的成果，仅作为历史快照。

判断当前实现、进行开发和测试时，只以 `RaspberryPi/` 与 `Uniforest_A/` 为准。`备份/` 中的代码不属于当前实现，默认只读，不主动修改、重构或参与全局搜索结论；需要恢复或对比历史版本时再按用户要求读取。

## 上位机

树莓派用户名为 `uniforest`；密码向队内管理员获取，不在公共仓库或项目文档中保存。

上位机代码位于 `RaspberryPi/`。修改算法、视觉、策略或通信程序时，应在该目录内完成，并同步维护相关文档和依赖声明。

## 下位机

下位机代码位于 `Uniforest_A/`，默认主程序为 `Uniforest_A/Core/Src/main.c`。这是持续迭代的核心电控工程，修改前应检查调用链、硬件影响和当前未提交变更。

统一使用 CLion 中配置的 OpenOCD + DAPLink 工具链进行编译、调试和烧录，不使用命令行直接烧录。命令行构建仅用于无硬件的编译验证。

## 开发与备份约定

- 日常开发只修改 `RaspberryPi/` 和 `Uniforest_A/`；不要把新功能直接开发在 `备份/` 中。
- 每天工作结束后，由用户将当天成果复制到 `备份/` 并使用清晰的日期或版本命名。
- 不覆盖已有备份；同一天需要多次快照时增加时间或序号。
- 文档与实现冲突时，以当前源码和构建配置为准，并同步修正文档。
- 不撤销或覆盖工作区中已有但与当前任务无关的修改。
